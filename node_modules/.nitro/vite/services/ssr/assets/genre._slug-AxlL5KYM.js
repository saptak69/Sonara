import { r as hashHue, t as Button } from "./button-D6cJemEt.js";
import { n as usePlayer } from "./cover-OSg2m9eM.js";
import { C as searchTracks, g as fetchTrending, r as Route, u as TrackRow } from "./router-B0lYCnsa.js";
import { t as AlbumCard } from "./cards-5QXYixfg.js";
import { t as HomeSkeleton } from "./home-skeleton-8YQEEbYz.js";
import { r as genreBySlug } from "./genres-HqDSRdv6.js";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
//#region src/routes/genre.$slug.tsx?tsr-split=component
function GenrePage() {
	const { slug } = Route.useParams();
	const genre = genreBySlug(slug);
	const playTracks = usePlayer((s) => s.playTracks);
	const query = useQuery({
		queryKey: ["genre", slug],
		queryFn: async () => {
			if (!genre) return searchTracks(slug, 24);
			const trending = await fetchTrending(24, genre.api);
			if (trending.length) return trending;
			return searchTracks(genre.label, 24);
		}
	});
	if (query.isLoading) return /* @__PURE__ */ jsx(HomeSkeleton, {});
	const tracks = query.data ?? [];
	const tone = hashHue(slug);
	return /* @__PURE__ */ jsxs("div", {
		className: "px-4 py-6 md:px-8",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "mb-8 overflow-hidden rounded-xl px-6 py-10",
			style: { background: `linear-gradient(135deg, hsl(${tone} 32% 28%), hsl(${tone} 16% 8%))` },
			children: [
				/* @__PURE__ */ jsx("p", {
					className: "text-xs font-medium tracking-[0.16em] text-fg/70 uppercase",
					children: "Genre"
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "mt-1 text-4xl font-semibold tracking-tight",
					children: genre?.label ?? slug
				}),
				genre?.hint ? /* @__PURE__ */ jsx("p", {
					className: "mt-2 text-sm text-fg/70",
					children: genre.hint
				}) : null,
				tracks.length ? /* @__PURE__ */ jsx(Button, {
					variant: "solid",
					className: "mt-5",
					onClick: () => playTracks(tracks, 0),
					children: "Play all"
				}) : null
			]
		}), tracks.length ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
			className: "mb-8 flex gap-4 overflow-x-auto [scrollbar-width:none]",
			children: tracks.slice(0, 10).map((t) => /* @__PURE__ */ jsx(AlbumCard, {
				track: t,
				queue: tracks
			}, t.id))
		}), tracks.map((t, i) => /* @__PURE__ */ jsx(TrackRow, {
			track: t,
			index: i,
			queue: tracks,
			showPlays: true
		}, `row-${t.id}`))] }) : /* @__PURE__ */ jsx("p", {
			className: "text-sm text-muted",
			children: "Nothing in this lane yet."
		})]
	});
}
//#endregion
export { GenrePage as component };
