import { t as Button } from "./button-D6cJemEt.js";
import { n as usePlayer } from "./cover-OSg2m9eM.js";
import { h as fetchRadioStations, y as radioToTrack } from "./router-B0lYCnsa.js";
import { a as RadioCard } from "./cards-5QXYixfg.js";
import { t as HomeSkeleton } from "./home-skeleton-8YQEEbYz.js";
import { t as Rail } from "./rail-D5jx3uJq.js";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
//#region src/routes/radio.tsx?tsr-split=component
var TAGS = [
	"pop",
	"jazz",
	"classical",
	"electronic",
	"news",
	"chill"
];
function RadioTagRail({ tag }) {
	const stations = useQuery({
		queryKey: ["radio-tag", tag],
		queryFn: () => fetchRadioStations(12, tag)
	}).data ?? [];
	if (!stations.length) return null;
	return /* @__PURE__ */ jsx(Rail, {
		title: tag[0].toUpperCase() + tag.slice(1),
		children: stations.map((s) => /* @__PURE__ */ jsx(RadioCard, { station: s }, s.id))
	});
}
function RadioPage() {
	const playTracks = usePlayer((s) => s.playTracks);
	const popular = useQuery({
		queryKey: ["radio-popular"],
		queryFn: () => fetchRadioStations(24)
	});
	if (popular.isLoading && !popular.data) return /* @__PURE__ */ jsx(HomeSkeleton, {});
	const all = popular.data ?? [];
	return /* @__PURE__ */ jsxs("div", {
		className: "stagger-in space-y-10 px-4 py-6 md:px-8",
		children: [
			/* @__PURE__ */ jsxs("header", {
				className: "flex items-end justify-between gap-4",
				children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
					className: "text-xs font-medium tracking-[0.18em] text-subtle uppercase",
					children: "On air"
				}), /* @__PURE__ */ jsx("h1", {
					className: "mt-1 text-3xl font-semibold tracking-tight",
					children: "Radio"
				})] }), all.length ? /* @__PURE__ */ jsx(Button, {
					variant: "chip",
					size: "sm",
					onClick: () => playTracks(all.map(radioToTrack), 0),
					children: "Play popular"
				}) : null]
			}),
			all.length ? /* @__PURE__ */ jsx(Rail, {
				title: "Popular stations",
				children: all.map((s) => /* @__PURE__ */ jsx(RadioCard, { station: s }, s.id))
			}) : /* @__PURE__ */ jsx("p", {
				className: "text-sm text-muted",
				children: "No stations right now."
			}),
			TAGS.map((tag) => /* @__PURE__ */ jsx(RadioTagRail, { tag }, tag))
		]
	});
}
//#endregion
export { RadioPage as component };
