import { a as getServerFnById, i as TSS_SERVER_FUNCTION, r as createServerFn, s as __exportAll } from "../index.js";
import { t as authMiddleware } from "./middleware-QvGm5jd5.js";
import { i as signOut, t as authClient } from "./client-B40BzJxt.js";
import { t as auth } from "./server-CVSnNY8q.js";
import { n as cn, r as hashHue, t as Button } from "./button-D6cJemEt.js";
import { n as usePlayer, t as Cover } from "./cover-OSg2m9eM.js";
import { useEffect, useMemo, useRef, useState } from "react";
import { HeadContent, Link, Outlet, Scripts, createFileRoute, createRootRoute, createRouter, lazyRouteComponent, useNavigate, useRouterState } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { randomUUID } from "node:crypto";
import { z } from "zod";
import { ChevronDown, Compass, Disc, Heart, Home, Library, ListEnd, ListMusic, ListPlus, LogOut, Mic2, MoreVertical, Pause, Play, Plus, Radio, Repeat, Repeat1, Search, Shuffle, SkipBack, SkipForward, TriangleAlert, User, UserRound, Volume2, VolumeX, X } from "lucide-react";
import { QueryClient, QueryClientProvider, useQuery } from "@tanstack/react-query";
import { Toaster, toast } from "sonner";
import * as Dropdown from "@radix-ui/react-dropdown-menu";
import * as D from "@radix-ui/react-dialog";
import { mkdir, writeFile } from "node:fs/promises";
import { join } from "node:path";
//#region src/lib/error-component.tsx
function AppErrorComponent({ error }) {
	return /* @__PURE__ */ jsxs("main", {
		className: "flex min-h-screen flex-col items-center justify-center gap-3 px-6 text-center bg-zinc-50 text-zinc-900 dark:bg-zinc-950 dark:text-zinc-50",
		children: [
			/* @__PURE__ */ jsx("span", {
				className: "text-red-500",
				"aria-hidden": "true",
				children: /* @__PURE__ */ jsx(TriangleAlert, {
					className: "size-10",
					strokeWidth: 2
				})
			}),
			/* @__PURE__ */ jsx("h1", {
				className: "text-lg font-semibold",
				children: "Something went wrong"
			}),
			/* @__PURE__ */ jsx("p", {
				className: "max-w-md text-sm break-words text-zinc-500 dark:text-zinc-400",
				children: error.message || "An unexpected error occurred. Try reloading the page."
			})
		]
	});
}
//#endregion
//#region src/lib/auth/provider.tsx
/**
* App-wide client provider mounted once near the root (in `src/routes/__root.tsx`):
*
*   <AuthProvider><Outlet /></AuthProvider>
*
* Better Auth's React client (`@/lib/auth/client`) needs NO context provider —
* its `useSession()` works standalone — so this is a passthrough today. It's
* kept as the single, stable mount point for any future client-side providers
* (e.g. a toast or theme provider) without churning the root shell.
*/
function AuthProvider({ children }) {
	return /* @__PURE__ */ jsx(Fragment, { children });
}
//#endregion
//#region src/components/preview-host-bridge.tsx
function PreviewHostBridge() {
	return null;
}
//#endregion
//#region src/components/logo.tsx
function Logo({ compact }) {
	return /* @__PURE__ */ jsxs(Link, {
		to: "/",
		className: "flex items-center gap-2.5 rounded-md outline-none focus-visible:ring-2 focus-visible:ring-fg/40",
		children: [/* @__PURE__ */ jsx("span", {
			className: "grid size-8 place-items-center rounded-md bg-accent text-accent-fg shadow-[0_6px_16px_rgb(255_42_61/0.28)]",
			children: /* @__PURE__ */ jsx("svg", {
				viewBox: "0 0 24 24",
				className: "size-5",
				fill: "currentColor",
				"aria-hidden": true,
				children: /* @__PURE__ */ jsx("path", { d: "M10 18.2a2.6 2.6 0 1 1-2.1-2.55V6.7c0-.5.34-.93.83-1.04l9-2.05A.85.85 0 0 1 19 4.45v9.2a2.6 2.6 0 1 1-2.1-2.55V7.15L10 8.75v9.45Z" })
			})
		}), /* @__PURE__ */ jsx("span", {
			className: cn("text-xl font-bold tracking-tight text-fg", compact && "sr-only"),
			children: "Sonara"
		})]
	});
}
//#endregion
//#region node_modules/@tanstack/start-server-core/dist/esm/createSsrRpc.js
var createSsrRpc = (functionId) => {
	const url = "/_serverFn/" + functionId;
	const serverFnMeta = { id: functionId };
	const fn = async (...args) => {
		return (await getServerFnById(functionId, { origin: "server" }))(...args);
	};
	return Object.assign(fn, {
		url,
		serverFnMeta,
		[TSS_SERVER_FUNCTION]: true
	});
};
//#endregion
//#region src/lib/artist-studio.ts
var getOrCreateArtistProfileServerFn = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(createSsrRpc("110b24a38fd13c76017a39eab3c958d525d9c2fd2a511b2df4f2ac2971a669bf"));
var updateArtistProfileServerFn = createServerFn({ method: "POST" }).middleware([authMiddleware]).validator((data) => {
	return z.object({
		displayName: z.string().min(1).max(80),
		handle: z.string().min(2).max(40).regex(/^[a-zA-Z0-9_]+$/, "Handle must be alphanumeric"),
		bio: z.string().max(500).optional().nullable(),
		avatarUrl: z.string().optional().nullable(),
		bannerUrl: z.string().optional().nullable()
	}).parse(data);
}).handler(createSsrRpc("d65f7f0255fb13f56baa0d5b1a33f91155fa1b89c23246b7d4e87874642c0892"));
var publishTrackServerFn = createServerFn({ method: "POST" }).middleware([authMiddleware]).validator((data) => {
	return z.object({
		title: z.string().min(1, "Track title is required").max(120),
		genre: z.string().optional().nullable(),
		mood: z.string().optional().nullable(),
		description: z.string().max(1e3).optional().nullable(),
		lyrics: z.string().max(1e4).optional().nullable(),
		coverArtUrl: z.string().optional().nullable(),
		audioUrl: z.string().min(1, "Audio file is required"),
		storageKey: z.string().min(1, "Storage key is required"),
		duration: z.number().nonnegative().default(0),
		fileSize: z.number().nonnegative().default(0),
		mimeType: z.string().optional().nullable(),
		status: z.enum(["published", "draft"]).default("published"),
		rightsConfirmed: z.boolean().refine((val) => val === true, { message: "You must confirm you own/control the rights to this recording." })
	}).parse(data);
}).handler(createSsrRpc("c4b4f878a61d581db13f310d02fba378db874717fcc5da8a1bdd9945e9e00b46"));
var getMyArtistTracksServerFn = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(createSsrRpc("53fb449ee8d964496d3909ccf7392cdba74c41b03878ae4fa2998be061dde6fe"));
var deleteArtistTrackServerFn = createServerFn({ method: "POST" }).middleware([authMiddleware]).validator((data) => {
	return z.object({ trackId: z.string() }).parse(data);
}).handler(createSsrRpc("61732241fa850e89215188ca09463c74f76ab8d3d1c3c05e1b1b1ed827d250d6"));
var recordStreamServerFn = createServerFn({ method: "POST" }).validator((data) => {
	return z.object({
		trackId: z.string(),
		sessionId: z.string().min(8),
		durationPlayed: z.number().min(30, "Stream must reach 30 seconds threshold")
	}).parse(data);
}).handler(createSsrRpc("0aa5139c56de2d1d73dee54876bce24d19afac5d63f334e8b49bbbe3708c107d"));
var getCommunityReleasesServerFn = createServerFn({ method: "GET" }).validator((data) => {
	return z.object({
		limit: z.number().min(1).max(50).default(20),
		genre: z.string().optional()
	}).optional().parse(data);
}).handler(createSsrRpc("42dac2dccdec93e92959663d68778ff535f8d26e9cdf520f746e1a71440d85cd"));
var getPublicArtistProfileServerFn = createServerFn({ method: "GET" }).validator((data) => {
	return z.object({ idOrHandle: z.string() }).parse(data);
}).handler(createSsrRpc("78015c7d8b65490bb75e007d6d76449dd3e2719b397a0e9e86ec6f4865c064a7"));
var getPublicArtistTracksServerFn = createServerFn({ method: "GET" }).validator((data) => {
	return z.object({
		artistId: z.string(),
		limit: z.number().min(1).max(100).default(50)
	}).parse(data);
}).handler(createSsrRpc("53510ff073fd7ac4bd69b7b9642f75e3cf472b6d3db5c7ce7e37bbaf85e58aee"));
//#endregion
//#region src/components/player/engine.tsx
function PlayerEngine() {
	const audioRef = useRef(null);
	const current = usePlayer((s) => s.queue[s.index]);
	const isPlaying = usePlayer((s) => s.isPlaying);
	const volume = usePlayer((s) => s.volume);
	const muted = usePlayer((s) => s.muted);
	const pendingSeek = usePlayer((s) => s.pendingSeek);
	const setCurrentTime = usePlayer((s) => s.setCurrentTime);
	const setDuration = usePlayer((s) => s.setDuration);
	const setPlaying = usePlayer((s) => s.setPlaying);
	const next = usePlayer((s) => s.next);
	const prev = usePlayer((s) => s.prev);
	const toggle = usePlayer((s) => s.toggle);
	const clearPendingSeek = usePlayer((s) => s.clearPendingSeek);
	const sessionIdRef = useRef(typeof window !== "undefined" ? window.sessionStorage.getItem("sonara_session_id") || (() => {
		const sid = "sess_" + Math.random().toString(36).substring(2, 15);
		window.sessionStorage.setItem("sonara_session_id", sid);
		return sid;
	})() : "sess_default");
	const countedTracksRef = useRef(/* @__PURE__ */ new Set());
	const playTimeRef = useRef(0);
	useEffect(() => {
		playTimeRef.current = 0;
	}, [current?.id]);
	useEffect(() => {
		if (!isPlaying || !current?.id) return;
		const interval = setInterval(() => {
			playTimeRef.current += 1;
			if (playTimeRef.current >= 30 && current.id.startsWith("track_") && !countedTracksRef.current.has(current.id)) {
				countedTracksRef.current.add(current.id);
				recordStreamServerFn({ data: {
					trackId: current.id,
					sessionId: sessionIdRef.current,
					durationPlayed: playTimeRef.current
				} }).catch(() => {});
			}
		}, 1e3);
		return () => clearInterval(interval);
	}, [isPlaying, current?.id]);
	useEffect(() => {
		const audio = audioRef.current;
		if (!audio || !current) return;
		if (audio.src !== current.streamUrl) {
			audio.src = current.streamUrl;
			audio.load();
		}
	}, [current?.id, current?.streamUrl]);
	useEffect(() => {
		const audio = audioRef.current;
		if (!audio) return;
		if (isPlaying) {
			const play = audio.play();
			if (play) play.catch(() => setPlaying(false));
		} else audio.pause();
	}, [
		isPlaying,
		current?.id,
		setPlaying
	]);
	useEffect(() => {
		const audio = audioRef.current;
		if (!audio) return;
		audio.volume = volume;
		audio.muted = muted;
	}, [volume, muted]);
	useEffect(() => {
		const audio = audioRef.current;
		if (!audio || pendingSeek == null) return;
		try {
			audio.currentTime = pendingSeek;
		} catch {}
		clearPendingSeek();
	}, [pendingSeek, clearPendingSeek]);
	useEffect(() => {
		if (!audioRef.current || !current) return;
		if ("mediaSession" in navigator) {
			navigator.mediaSession.metadata = new MediaMetadata({
				title: current.title,
				artist: current.artist,
				artwork: current.artworkLg ? [{
					src: current.artworkLg,
					sizes: "512x512",
					type: "image/jpeg"
				}] : []
			});
			navigator.mediaSession.playbackState = isPlaying ? "playing" : "paused";
			navigator.mediaSession.setActionHandler("play", () => setPlaying(true));
			navigator.mediaSession.setActionHandler("pause", () => setPlaying(false));
			navigator.mediaSession.setActionHandler("previoustrack", () => prev());
			navigator.mediaSession.setActionHandler("nexttrack", () => next());
		}
	}, [
		current,
		isPlaying,
		next,
		prev,
		setPlaying
	]);
	useEffect(() => {
		const onKey = (e) => {
			const tag = e.target?.tagName;
			if (tag === "INPUT" || tag === "TEXTAREA" || e.target?.isContentEditable) return;
			if (e.code === "Space") {
				e.preventDefault();
				toggle();
			} else if (e.code === "ArrowRight") next();
			else if (e.code === "ArrowLeft") prev();
			else if (e.code === "KeyM") usePlayer.getState().toggleMute();
		};
		window.addEventListener("keydown", onKey);
		return () => window.removeEventListener("keydown", onKey);
	}, [
		next,
		prev,
		toggle
	]);
	const lastErrorRef = useRef(0);
	return /* @__PURE__ */ jsx("audio", {
		ref: audioRef,
		preload: "metadata",
		onTimeUpdate: (e) => setCurrentTime(e.currentTarget.currentTime),
		onDurationChange: (e) => setDuration(e.currentTarget.duration || 0),
		onEnded: () => next(),
		onPlay: () => setPlaying(true),
		onPause: () => {
			if (usePlayer.getState().isPlaying) {}
		},
		onError: () => {
			const now = Date.now();
			if (now - lastErrorRef.current > 1500) {
				lastErrorRef.current = now;
				if (usePlayer.getState().isPlaying) next();
			} else setPlaying(false);
		}
	});
}
//#endregion
//#region src/lib/format.ts
function formatTime(seconds) {
	if (!Number.isFinite(seconds) || seconds < 0) return "0:00";
	const s = Math.floor(seconds);
	const h = Math.floor(s / 3600);
	const m = Math.floor(s % 3600 / 60);
	const r = s % 60;
	if (h > 0) return `${h}:${String(m).padStart(2, "0")}:${String(r).padStart(2, "0")}`;
	return `${m}:${String(r).padStart(2, "0")}`;
}
function formatCount(n) {
	if (n == null) return "";
	if (n >= 1e6) return `${(n / 1e6).toFixed(n >= 1e7 ? 0 : 1)}M`;
	if (n >= 1e3) return `${(n / 1e3).toFixed(n >= 1e4 ? 0 : 1)}K`;
	return String(n);
}
function formatDurationTotal(seconds) {
	if (!Number.isFinite(seconds) || seconds <= 0) return "";
	const h = Math.floor(seconds / 3600);
	const m = Math.round(seconds % 3600 / 60);
	if (h > 0) return `${h} hr ${m} min`;
	return `${Math.max(1, m)} min`;
}
//#endregion
//#region src/components/player/bar.tsx
function IconSwap({ on, OnIcon, OffIcon }) {
	return /* @__PURE__ */ jsxs("span", {
		className: "relative grid size-5 place-items-center",
		children: [/* @__PURE__ */ jsx(OnIcon, { className: cn("absolute size-5 transition-[opacity,transform,filter] duration-300 ease-[cubic-bezier(0.2,0,0,1)]", on ? "scale-100 opacity-100 blur-none" : "scale-[0.25] opacity-0 blur-[4px]") }), /* @__PURE__ */ jsx(OffIcon, { className: cn("size-5 transition-[opacity,transform,filter] duration-300 ease-[cubic-bezier(0.2,0,0,1)]", on ? "scale-[0.25] opacity-0 blur-[4px]" : "scale-100 opacity-100 blur-none") })]
	});
}
function PlayerBar() {
	const track = usePlayer((s) => s.queue[s.index]);
	const isPlaying = usePlayer((s) => s.isPlaying);
	const currentTime = usePlayer((s) => s.currentTime);
	const duration = usePlayer((s) => s.duration);
	const volume = usePlayer((s) => s.volume);
	const muted = usePlayer((s) => s.muted);
	const shuffle = usePlayer((s) => s.shuffle);
	const repeat = usePlayer((s) => s.repeat);
	const likedIds = usePlayer((s) => s.likedIds);
	const liked = Boolean(track && likedIds.includes(track.id));
	const toggle = usePlayer((s) => s.toggle);
	const next = usePlayer((s) => s.next);
	const prev = usePlayer((s) => s.prev);
	const seekTo = usePlayer((s) => s.seekTo);
	const setVolume = usePlayer((s) => s.setVolume);
	const toggleMute = usePlayer((s) => s.toggleMute);
	const toggleShuffle = usePlayer((s) => s.toggleShuffle);
	const cycleRepeat = usePlayer((s) => s.cycleRepeat);
	const toggleLike = usePlayer((s) => s.toggleLike);
	const setExpanded = usePlayer((s) => s.setExpanded);
	const setQueueOpen = usePlayer((s) => s.setQueueOpen);
	const setLyricsOpen = usePlayer((s) => s.setLyricsOpen);
	const queueOpen = usePlayer((s) => s.queueOpen);
	const lyricsOpen = usePlayer((s) => s.lyricsOpen);
	const live = track?.kind === "radio";
	const progress = live || !duration || !Number.isFinite(duration) ? 0 : currentTime / duration * 100;
	if (!track) return /* @__PURE__ */ jsx("div", {
		className: "pointer-events-none hidden h-player md:block",
		"aria-hidden": true
	});
	return /* @__PURE__ */ jsxs("div", {
		className: "border-t border-white/10 bg-black/60 backdrop-blur-3xl shadow-2xl",
		children: [!live && duration > 0 ? /* @__PURE__ */ jsx("div", {
			className: "h-0.5 w-full bg-white/10 md:hidden",
			children: /* @__PURE__ */ jsx("div", {
				className: "h-full bg-accent transition-all duration-150",
				style: { width: `${progress}%` }
			})
		}) : null, /* @__PURE__ */ jsxs("div", {
			className: "flex h-player items-center gap-2.5 px-3 md:gap-6 md:px-6",
			children: [
				/* @__PURE__ */ jsxs("button", {
					type: "button",
					className: "flex min-w-0 flex-1 items-center gap-2.5 text-left md:max-w-xs lg:max-w-sm active:scale-98 transition-transform",
					onClick: () => setExpanded(true),
					children: [/* @__PURE__ */ jsx(Cover, {
						src: track.artwork,
						alt: track.title,
						title: track.title,
						className: "size-11 md:size-12 shrink-0 rounded-lg shadow-sm"
					}), /* @__PURE__ */ jsxs("span", {
						className: "min-w-0 flex-1",
						children: [/* @__PURE__ */ jsx("span", {
							className: "block truncate text-xs md:text-sm font-semibold text-fg",
							children: track.title
						}), track.artistId ? /* @__PURE__ */ jsx(Link, {
							to: "/artist/$id",
							params: { id: track.artistId },
							className: "block truncate text-[11px] md:text-xs text-muted hover:text-fg hover:underline",
							onClick: (e) => e.stopPropagation(),
							children: track.artist
						}) : /* @__PURE__ */ jsx("span", {
							className: "block truncate text-[11px] md:text-xs text-muted",
							children: track.artist
						})]
					})]
				}),
				/* @__PURE__ */ jsx(Button, {
					variant: "icon",
					size: "iconSm",
					"aria-label": liked ? "Unlike" : "Like",
					className: "grid shrink-0 text-muted hover:text-fg",
					onClick: () => toggleLike(track),
					children: /* @__PURE__ */ jsx(Heart, { className: cn("size-4 md:size-4.5", liked && "fill-accent text-accent") })
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "flex shrink-0 md:min-w-0 md:flex-[1.4] flex-col items-center gap-1",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-1 md:gap-2",
						children: [
							/* @__PURE__ */ jsx(Button, {
								variant: "icon",
								size: "iconSm",
								"aria-label": "Shuffle",
								className: cn("hidden md:grid", shuffle && "text-accent"),
								onClick: toggleShuffle,
								children: /* @__PURE__ */ jsx(Shuffle, { className: "size-4" })
							}),
							/* @__PURE__ */ jsx(Button, {
								variant: "icon",
								size: "iconSm",
								"aria-label": "Previous",
								onClick: prev,
								children: /* @__PURE__ */ jsx(SkipBack, { className: "size-5 fill-current" })
							}),
							/* @__PURE__ */ jsx(Button, {
								variant: "solid",
								size: "icon",
								"aria-label": isPlaying ? "Pause" : "Play",
								className: "size-10",
								onClick: toggle,
								children: /* @__PURE__ */ jsx(IconSwap, {
									on: isPlaying,
									OnIcon: Pause,
									OffIcon: Play
								})
							}),
							/* @__PURE__ */ jsx(Button, {
								variant: "icon",
								size: "iconSm",
								"aria-label": "Next",
								onClick: next,
								children: /* @__PURE__ */ jsx(SkipForward, { className: "size-5 fill-current" })
							}),
							/* @__PURE__ */ jsx(Button, {
								variant: "icon",
								size: "iconSm",
								"aria-label": "Repeat",
								className: cn("hidden md:grid", repeat !== "off" && "text-accent"),
								onClick: cycleRepeat,
								children: repeat === "one" ? /* @__PURE__ */ jsx(Repeat1, { className: "size-4" }) : /* @__PURE__ */ jsx(Repeat, { className: "size-4" })
							})
						]
					}), /* @__PURE__ */ jsxs("div", {
						className: "hidden w-full max-w-xl items-center gap-2 md:flex",
						children: [
							/* @__PURE__ */ jsx("span", {
								className: "w-10 text-right text-[11px] tabular-nums text-subtle",
								children: live ? "LIVE" : formatTime(currentTime)
							}),
							/* @__PURE__ */ jsx("input", {
								className: "player-range",
								type: "range",
								min: 0,
								max: live || !duration ? 0 : duration,
								step: .1,
								value: live ? 0 : currentTime,
								disabled: live,
								style: { "--progress": `${progress}%` },
								onChange: (e) => seekTo(Number(e.target.value)),
								"aria-label": "Seek"
							}),
							/* @__PURE__ */ jsx("span", {
								className: "w-10 text-[11px] tabular-nums text-subtle",
								children: live ? "" : formatTime(Number.isFinite(duration) ? duration : 0)
							})
						]
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "hidden items-center gap-1 lg:flex",
					children: [
						/* @__PURE__ */ jsx(Button, {
							variant: "icon",
							size: "iconSm",
							"aria-label": "Lyrics",
							className: cn(lyricsOpen && "text-accent"),
							onClick: () => {
								setExpanded(true);
								setLyricsOpen(!lyricsOpen);
							},
							children: /* @__PURE__ */ jsx(Mic2, { className: "size-4" })
						}),
						/* @__PURE__ */ jsx(Button, {
							variant: "icon",
							size: "iconSm",
							"aria-label": "Queue",
							className: cn(queueOpen && "text-accent"),
							onClick: () => setQueueOpen(!queueOpen),
							children: /* @__PURE__ */ jsx(ListMusic, { className: "size-4" })
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "group/vol flex items-center",
							children: [/* @__PURE__ */ jsx(Button, {
								variant: "icon",
								size: "iconSm",
								"aria-label": muted ? "Unmute" : "Mute",
								onClick: toggleMute,
								children: /* @__PURE__ */ jsx(IconSwap, {
									on: muted || volume === 0,
									OnIcon: VolumeX,
									OffIcon: Volume2
								})
							}), /* @__PURE__ */ jsx("input", {
								className: "player-range w-0 opacity-0 transition-[width,opacity] duration-200 group-hover/vol:w-24 group-hover/vol:opacity-100",
								type: "range",
								min: 0,
								max: 1,
								step: .01,
								value: muted ? 0 : volume,
								style: { "--progress": `${(muted ? 0 : volume) * 100}%` },
								onChange: (e) => setVolume(Number(e.target.value)),
								"aria-label": "Volume"
							})]
						})
					]
				})
			]
		})]
	});
}
//#endregion
//#region src/lib/music-api.ts
var APP = "sonara_music";
var AUDIUS_NODES = ["https://discoveryprovider.audius.co/v1", "https://api.audius.co/v1"];
var RADIO_NODES = ["https://de1.api.radio-browser.info/json", "https://at1.api.radio-browser.info/json"];
var CURATED_TRACKS = [
	{
		id: "curated_midnight_drive",
		title: "Midnight City Lights",
		artist: "Neon Skyline",
		artwork: "https://images.unsplash.com/photo-1518709268805-4e9042af9f23?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1518709268805-4e9042af9f23?w=1000&h=1000&fit=crop&q=85",
		duration: 214,
		streamUrl: "https://cdn.pixabay.com/download/audio/2022/05/27/audio_1808fbf07a.mp3?filename=midnight-forest-184304.mp3",
		genre: "Synthwave",
		mood: "Chill",
		playCount: 148200,
		kind: "track"
	},
	{
		id: "curated_lofi_rain",
		title: "Late Night Rain Beats",
		artist: "Aura Chill",
		artwork: "https://images.unsplash.com/photo-1534447677768-be436bb09401?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1534447677768-be436bb09401?w=1000&h=1000&fit=crop&q=85",
		duration: 185,
		streamUrl: "https://cdn.pixabay.com/download/audio/2022/01/18/audio_d0a13f69d2.mp3?filename=lofi-study-112191.mp3",
		genre: "Lo-Fi",
		mood: "Relaxed",
		playCount: 239400,
		kind: "track"
	},
	{
		id: "curated_cyber_drift",
		title: "Neon Cyber Drift",
		artist: "RetroWave Echo",
		artwork: "https://images.unsplash.com/photo-1508700115892-45ecd05ae2ad?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1508700115892-45ecd05ae2ad?w=1000&h=1000&fit=crop&q=85",
		duration: 230,
		streamUrl: "https://cdn.pixabay.com/download/audio/2022/03/15/audio_c8c8a73467.mp3?filename=the-cradle-of-your-soul-15700.mp3",
		genre: "Electronic",
		mood: "Energetic",
		playCount: 195e3,
		kind: "track"
	},
	{
		id: "curated_cozy_coffee",
		title: "Sunday Coffee & Vinyl",
		artist: "Komorebi Duo",
		artwork: "https://images.unsplash.com/photo-1501386761578-eac5c94b800a?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1501386761578-eac5c94b800a?w=1000&h=1000&fit=crop&q=85",
		duration: 198,
		streamUrl: "https://cdn.pixabay.com/download/audio/2022/10/14/audio_9939f792cb.mp3?filename=smoke-143172.mp3",
		genre: "Jazz / Chill",
		mood: "Peaceful",
		playCount: 312e3,
		kind: "track"
	},
	{
		id: "curated_stellar_pulse",
		title: "Stellar Journey",
		artist: "Cosmic Odyssey",
		artwork: "https://images.unsplash.com/photo-1451187580459-43490279c0fa?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1451187580459-43490279c0fa?w=1000&h=1000&fit=crop&q=85",
		duration: 245,
		streamUrl: "https://cdn.pixabay.com/download/audio/2022/03/10/audio_c3527e30de.mp3?filename=space-atmosphere-10612.mp3",
		genre: "Ambient",
		mood: "Dreamy",
		playCount: 88400,
		kind: "track"
	},
	{
		id: "curated_urban_groove",
		title: "Sunset Boulevard",
		artist: "Velvet Groove",
		artwork: "https://images.unsplash.com/photo-1470225620780-dba8ba36b745?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1470225620780-dba8ba36b745?w=1000&h=1000&fit=crop&q=85",
		duration: 210,
		streamUrl: "https://cdn.pixabay.com/download/audio/2021/08/04/audio_bb630d7a3f.mp3?filename=watr-fluid-10149.mp3",
		genre: "Pop / R&B",
		mood: "Upbeat",
		playCount: 42e4,
		kind: "track"
	},
	{
		id: "curated_tokyo_drift",
		title: "Tokyo Shinjuku Nights",
		artist: "Shibuya Beats",
		artwork: "https://images.unsplash.com/photo-1503899036084-c55cdd92da26?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1503899036084-c55cdd92da26?w=1000&h=1000&fit=crop&q=85",
		duration: 178,
		streamUrl: "https://cdn.pixabay.com/download/audio/2022/01/26/audio_d0c6ff1101.mp3?filename=electronic-future-beats-117997.mp3",
		genre: "Electronic",
		mood: "Focus",
		playCount: 275e3,
		kind: "track"
	},
	{
		id: "curated_golden_hour",
		title: "Golden Hour Glow",
		artist: "Solstice Breeze",
		artwork: "https://images.unsplash.com/photo-1514525253161-7a46d19cd819?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1514525253161-7a46d19cd819?w=1000&h=1000&fit=crop&q=85",
		duration: 202,
		streamUrl: "https://cdn.pixabay.com/download/audio/2022/11/06/audio_c896e382b6.mp3?filename=warm-breeze-126298.mp3",
		genre: "Acoustic / Indie",
		mood: "Warm",
		playCount: 164e3,
		kind: "track"
	}
];
var CURATED_RADIO = [
	{
		id: "somafm_groovesalad",
		name: "SomaFM Groove Salad",
		artwork: "https://somafm.com/img3/groovesalad-400.jpg",
		streamUrl: "https://ice1.somafm.com/groovesalad-128-mp3",
		tags: "chill,ambient,downtempo,lounge",
		country: "United States",
		bitrate: 128
	},
	{
		id: "somafm_secretagent",
		name: "SomaFM Secret Agent",
		artwork: "https://somafm.com/img3/secretagent-400.jpg",
		streamUrl: "https://ice1.somafm.com/secretagent-128-mp3",
		tags: "spy,lounge,surf,jazz",
		country: "United States",
		bitrate: 128
	},
	{
		id: "somafm_dronezone",
		name: "SomaFM Drone Zone",
		artwork: "https://somafm.com/img3/dronezone-400.jpg",
		streamUrl: "https://ice1.somafm.com/dronezone-128-mp3",
		tags: "ambient,space,drone",
		country: "United States",
		bitrate: 128
	},
	{
		id: "somafm_indiepop",
		name: "SomaFM Indie Pop Rocks!",
		artwork: "https://somafm.com/img3/indiepop-400.jpg",
		streamUrl: "https://ice1.somafm.com/indiepop-128-mp3",
		tags: "indie,pop,rock,alternative",
		country: "United States",
		bitrate: 128
	},
	{
		id: "smooth_jazz_live",
		name: "Smooth Jazz 24/7",
		artwork: "https://images.unsplash.com/photo-1511192336575-5a79af67a629?w=480&h=480&fit=crop&q=80",
		streamUrl: "https://smoothjazz.cdnstream1.com/2585_128.mp3",
		tags: "jazz,smooth,relax",
		country: "United States",
		bitrate: 128
	},
	{
		id: "dance_wave_fm",
		name: "Dance Wave Hits",
		artwork: "https://images.unsplash.com/photo-1492684223066-81342ee5ff30?w=480&h=480&fit=crop&q=80",
		streamUrl: "https://dancewave.online/dance.mp3",
		tags: "dance,club,electronic,edm",
		country: "Hungary",
		bitrate: 128
	}
];
async function getJson(url, timeoutMs = 6e3) {
	const controller = new AbortController();
	const timer = setTimeout(() => controller.abort(), timeoutMs);
	try {
		const res = await fetch(url, { signal: controller.signal });
		if (!res.ok) throw new Error(`Request failed (${res.status})`);
		return await res.json();
	} finally {
		clearTimeout(timer);
	}
}
async function fetchFromAudius(path, params = {}) {
	const query = new URLSearchParams({
		app_name: APP,
		...params
	}).toString();
	for (const node of AUDIUS_NODES) try {
		const data = unwrap(await getJson(`${node}${path}?${query}`, 4500));
		if (data) return data;
	} catch {}
	return null;
}
function unwrap(payload) {
	if (payload && typeof payload === "object" && "data" in payload) return payload.data;
	return payload;
}
function pickArt(art, size) {
	if (!art) return null;
	const raw = art[size] || art["480x480"] || art["1000x1000"] || art["150x150"] || null;
	if (!raw) return null;
	return raw.startsWith("http://") ? raw.replace("http://", "https://") : raw;
}
function trackStreamUrl(id) {
	return `${AUDIUS_NODES[0]}/tracks/${encodeURIComponent(id)}/stream?app_name=${APP}`;
}
function mapTrack(t) {
	if (!t.id || !t.title) return null;
	if (t.is_streamable === false || t.is_stream_gated) return null;
	const artist = t.user?.name || t.user?.handle || "Unknown artist";
	return {
		id: t.id,
		title: t.title,
		artist,
		artistId: t.user?.id,
		album: void 0,
		artwork: pickArt(t.artwork ?? null, "150x150"),
		artworkLg: pickArt(t.artwork ?? null, "1000x1000") || pickArt(t.artwork ?? null, "480x480"),
		duration: Number(t.duration) || 0,
		streamUrl: trackStreamUrl(t.id),
		genre: t.genre,
		mood: t.mood,
		playCount: t.play_count,
		kind: "track"
	};
}
function mapPlaylist(p) {
	if (!p.id || !p.playlist_name) return null;
	const tracks = (p.tracks ?? []).map(mapTrack).filter((x) => Boolean(x));
	return {
		id: p.id,
		name: p.playlist_name,
		description: p.description || void 0,
		artwork: pickArt(p.artwork ?? null, "480x480"),
		artworkLg: pickArt(p.artwork ?? null, "1000x1000"),
		trackCount: p.track_count ?? tracks.length,
		isAlbum: Boolean(p.is_album),
		owner: p.user?.name,
		ownerId: p.user?.id,
		tracks: tracks.length ? tracks : void 0
	};
}
function mapArtist(u) {
	if (!u.id || !u.name) return null;
	return {
		id: u.id,
		name: u.name,
		handle: u.handle,
		bio: u.bio,
		artwork: pickArt(u.profile_picture ?? null, "480x480"),
		artworkLg: pickArt(u.profile_picture ?? null, "1000x1000"),
		followerCount: u.follower_count,
		trackCount: u.track_count
	};
}
async function fetchTrending(limit = 20, genre) {
	try {
		const params = { limit: String(limit) };
		if (genre) params.genre = genre;
		const mapped = (await fetchFromAudius("/tracks/trending", params) ?? []).map(mapTrack).filter((x) => Boolean(x));
		if (mapped.length >= 4) return mapped;
	} catch {}
	return CURATED_TRACKS.slice(0, limit);
}
async function fetchUnderground(limit = 16) {
	try {
		const mapped = (await fetchFromAudius("/tracks/trending/underground", { limit: String(limit) }) ?? []).map(mapTrack).filter((x) => Boolean(x));
		if (mapped.length >= 4) return mapped;
	} catch {}
	return [...CURATED_TRACKS].reverse().slice(0, limit);
}
async function fetchTrendingPlaylists(limit = 16) {
	try {
		const mapped = (await fetchFromAudius("/playlists/trending", { limit: String(limit) }) ?? []).map(mapPlaylist).filter((x) => Boolean(x));
		if (mapped.length >= 2) return mapped;
	} catch {}
	return [{
		id: "curated_pl_chill",
		name: "Saptak's Chill Vibes",
		description: "Atmospheric beats, late night lo-fi and deep melodic chill.",
		artwork: CURATED_TRACKS[1].artwork,
		artworkLg: CURATED_TRACKS[1].artworkLg,
		trackCount: CURATED_TRACKS.length,
		isAlbum: false,
		owner: "Saptak Mondal",
		tracks: CURATED_TRACKS
	}, {
		id: "curated_pl_synth",
		name: "Cyber & Synthwave Anthems",
		description: "High octane synth and retrofuturistic night rhythms.",
		artwork: CURATED_TRACKS[0].artwork,
		artworkLg: CURATED_TRACKS[0].artworkLg,
		trackCount: CURATED_TRACKS.length,
		isAlbum: false,
		owner: "Saptak Mondal",
		tracks: CURATED_TRACKS
	}];
}
async function searchTracks(query, limit = 24) {
	try {
		const mapped = (await fetchFromAudius("/tracks/search", {
			query,
			limit: String(limit)
		}) ?? []).map(mapTrack).filter((x) => Boolean(x));
		if (mapped.length) return mapped;
	} catch {}
	const q = query.toLowerCase();
	const matched = CURATED_TRACKS.filter((t) => t.title.toLowerCase().includes(q) || t.artist.toLowerCase().includes(q) || t.genre && t.genre.toLowerCase().includes(q));
	return matched.length ? matched : CURATED_TRACKS.slice(0, limit);
}
async function searchPlaylists(query, limit = 12) {
	try {
		const mapped = (await fetchFromAudius("/playlists/search", {
			query,
			limit: String(limit)
		}) ?? []).map(mapPlaylist).filter((x) => Boolean(x));
		if (mapped.length) return mapped;
	} catch {}
	return fetchTrendingPlaylists(limit);
}
async function searchArtists(query, limit = 12) {
	try {
		const mapped = (await fetchFromAudius("/users/search", {
			query,
			limit: String(limit)
		}) ?? []).map(mapArtist).filter((x) => Boolean(x));
		if (mapped.length) return mapped;
	} catch {}
	return [{
		id: "curated_artist_saptak",
		name: "Saptak Mondal",
		handle: "saptak",
		bio: "Creator and sound curator of Sonara Music Player.",
		artwork: "https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?w=1000&h=1000&fit=crop&q=85",
		followerCount: 9940,
		trackCount: CURATED_TRACKS.length
	}, {
		id: "curated_artist_neon",
		name: "Neon Skyline",
		handle: "neonskyline",
		bio: "Retro-synth and atmospheric electronic artist.",
		artwork: CURATED_TRACKS[0].artwork,
		artworkLg: CURATED_TRACKS[0].artworkLg,
		followerCount: 54200,
		trackCount: 12
	}];
}
async function fetchPlaylist(id) {
	if (id.startsWith("curated_pl_")) {
		const playlists = await fetchTrendingPlaylists(10);
		return playlists.find((p) => p.id === id) || playlists[0] || null;
	}
	try {
		const raw = await fetchFromAudius(`/playlists/${encodeURIComponent(id)}`);
		const data = Array.isArray(raw) ? raw[0] : raw;
		return data ? mapPlaylist(data) : null;
	} catch {
		return null;
	}
}
async function fetchArtist(id) {
	if (id === "curated_artist_saptak") return {
		id: "curated_artist_saptak",
		name: "Saptak Mondal",
		handle: "saptak",
		bio: "Creator and sound curator of Sonara Music Player.",
		artwork: "https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?w=480&h=480&fit=crop&q=80",
		artworkLg: "https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?w=1000&h=1000&fit=crop&q=85",
		followerCount: 9940,
		trackCount: CURATED_TRACKS.length
	};
	try {
		const raw = await fetchFromAudius(`/users/${encodeURIComponent(id)}`);
		const data = Array.isArray(raw) ? raw[0] : raw;
		return data ? mapArtist(data) : null;
	} catch {
		return null;
	}
}
async function fetchArtistTracks(id, limit = 40) {
	if (id.startsWith("curated_")) return CURATED_TRACKS;
	try {
		const mapped = (await fetchFromAudius(`/users/${encodeURIComponent(id)}/tracks`, { limit: String(limit) }) ?? []).map(mapTrack).filter((x) => Boolean(x));
		if (mapped.length) return mapped;
	} catch {}
	return CURATED_TRACKS.slice(0, limit);
}
function mapRadio(s) {
	const stream = s.url_resolved || s.url;
	if (!s.stationuuid || !s.name || !stream) return null;
	if (stream.startsWith("http://")) return null;
	return {
		id: s.stationuuid,
		name: s.name.trim(),
		artwork: s.favicon || null,
		streamUrl: stream,
		tags: s.tags || "",
		country: s.country || "",
		bitrate: s.bitrate
	};
}
function radioToTrack(station) {
	return {
		id: `radio:${station.id}`,
		title: station.name,
		artist: station.country || "Live radio",
		artwork: station.artwork,
		artworkLg: station.artwork,
		duration: 0,
		streamUrl: station.streamUrl,
		genre: station.tags.split(",")[0],
		kind: "radio"
	};
}
async function fetchRadioStations(limit = 24, tag) {
	for (const node of RADIO_NODES) try {
		const url = new URL(`${node}/stations/search`);
		url.searchParams.set("limit", String(limit));
		url.searchParams.set("hidebroken", "true");
		url.searchParams.set("order", "clickcount");
		url.searchParams.set("reverse", "true");
		if (tag) url.searchParams.set("tag", tag);
		const mapped = (await getJson(url.toString(), 4e3) ?? []).map(mapRadio).filter((x) => Boolean(x));
		if (mapped.length >= 3) {
			const seen = /* @__PURE__ */ new Set();
			return mapped.filter((s) => {
				const key = s.name.toLowerCase();
				if (seen.has(key)) return false;
				seen.add(key);
				return true;
			});
		}
	} catch {}
	if (tag) {
		const q = tag.toLowerCase();
		const filtered = CURATED_RADIO.filter((r) => r.tags.toLowerCase().includes(q));
		if (filtered.length) return filtered;
	}
	return CURATED_RADIO.slice(0, limit);
}
async function searchRadio(query, limit = 16) {
	for (const node of RADIO_NODES) try {
		const url = new URL(`${node}/stations/search`);
		url.searchParams.set("name", query);
		url.searchParams.set("limit", String(limit));
		url.searchParams.set("hidebroken", "true");
		url.searchParams.set("order", "clickcount");
		url.searchParams.set("reverse", "true");
		const mapped = (await getJson(url.toString(), 4e3) ?? []).map(mapRadio).filter((x) => Boolean(x));
		if (mapped.length) return mapped;
	} catch {}
	const q = query.toLowerCase();
	const matched = CURATED_RADIO.filter((s) => s.name.toLowerCase().includes(q) || s.tags.toLowerCase().includes(q));
	return matched.length ? matched : CURATED_RADIO.slice(0, limit);
}
function parseSynced(raw) {
	if (!raw) return null;
	const lines = [];
	for (const line of raw.split("\n")) {
		const m = line.match(/\[(\d{1,2}):(\d{2})(?:\.(\d{1,3}))?\](.*)/);
		if (!m) continue;
		const min = Number(m[1]);
		const sec = Number(m[2]);
		const ms = Number((m[3] ?? "0").padEnd(3, "0"));
		lines.push({
			time: min * 60 + sec + ms / 1e3,
			text: (m[4] ?? "").trim()
		});
	}
	return lines.length ? lines : null;
}
async function fetchLyrics(title, artist) {
	try {
		const url = new URL("https://lrclib.net/api/search");
		url.searchParams.set("track_name", title);
		url.searchParams.set("artist_name", artist.replace(/^@/, ""));
		const hit = (await getJson(url.toString(), 4e3) ?? []).find((d) => d.plainLyrics || d.syncedLyrics || d.instrumental);
		if (!hit) return null;
		return {
			plain: hit.plainLyrics || "",
			synced: parseSynced(hit.syncedLyrics),
			instrumental: Boolean(hit.instrumental)
		};
	} catch {
		return null;
	}
}
//#endregion
//#region src/components/player/lyrics.tsx
function LyricsPanel() {
	const track = usePlayer((s) => s.queue[s.index]);
	const currentTime = usePlayer((s) => s.currentTime);
	const open = usePlayer((s) => s.lyricsOpen);
	const activeRef = useRef(null);
	const { data, isLoading } = useQuery({
		queryKey: ["lyrics", track?.id],
		queryFn: () => fetchLyrics(track.title, track.artist),
		enabled: open && Boolean(track) && track?.kind !== "radio"
	});
	const activeIndex = data?.synced?.reduce((acc, line, i) => currentTime >= line.time ? i : acc, -1) ?? -1;
	useEffect(() => {
		activeRef.current?.scrollIntoView({
			behavior: "smooth",
			block: "center"
		});
	}, [activeIndex]);
	if (!open) return null;
	const customLyrics = track?.lyrics;
	return /* @__PURE__ */ jsx("div", {
		className: "mt-6 max-h-64 overflow-y-auto px-2 text-center [scrollbar-width:thin]",
		children: isLoading ? /* @__PURE__ */ jsx("p", {
			className: "text-sm text-muted",
			children: "Finding lyrics…"
		}) : data?.synced ? /* @__PURE__ */ jsx("div", {
			className: "space-y-3",
			children: data.synced.map((line, i) => /* @__PURE__ */ jsx("p", {
				ref: i === activeIndex ? activeRef : void 0,
				className: i === activeIndex ? "text-lg font-semibold text-fg" : "text-sm text-subtle transition-colors duration-200",
				children: line.text || " "
			}, `${line.time}-${i}`))
		}) : data?.plain ? /* @__PURE__ */ jsx("p", {
			className: "whitespace-pre-wrap text-sm leading-7 text-muted",
			children: data.plain
		}) : customLyrics ? /* @__PURE__ */ jsxs("div", {
			className: "space-y-2 text-center",
			children: [/* @__PURE__ */ jsx("p", {
				className: "text-xs uppercase tracking-wider text-accent font-semibold",
				children: "Artist Lyrics"
			}), /* @__PURE__ */ jsx("p", {
				className: "whitespace-pre-wrap text-sm leading-7 text-fg/90",
				children: customLyrics
			})]
		}) : data?.instrumental ? /* @__PURE__ */ jsx("p", {
			className: "text-sm text-muted",
			children: "Instrumental"
		}) : /* @__PURE__ */ jsx("p", {
			className: "text-sm text-muted",
			children: "Lyrics not available for this track."
		})
	});
}
//#endregion
//#region src/components/player/visualizer.tsx
function Visualizer() {
	const canvasRef = useRef(null);
	const isPlaying = usePlayer((s) => s.isPlaying);
	useEffect(() => {
		const canvas = canvasRef.current;
		if (!canvas) return;
		const ctx = canvas.getContext("2d");
		if (!ctx) return;
		let raf = 0;
		const bars = 48;
		const draw = (t) => {
			const { width, height } = canvas;
			ctx.clearRect(0, 0, width, height);
			const w = width / bars;
			for (let i = 0; i < bars; i += 1) {
				const wave = Math.sin(t / 240 + i * .35) * .5 + .5;
				const pulse = isPlaying ? .35 + wave * .65 : .12;
				const h = pulse * height * (.4 + i % 7 * .08);
				const x = i * w;
				ctx.fillStyle = `rgba(255,255,255,${.12 + pulse * .28})`;
				ctx.fillRect(x + 1, height - h, Math.max(2, w - 3), h);
			}
			raf = requestAnimationFrame(draw);
		};
		raf = requestAnimationFrame(draw);
		return () => cancelAnimationFrame(raf);
	}, [isPlaying]);
	return /* @__PURE__ */ jsx("canvas", {
		ref: canvasRef,
		width: 640,
		height: 72,
		className: "h-16 w-full opacity-80",
		"aria-hidden": true
	});
}
//#endregion
//#region src/lib/color.ts
async function extractAccent(urlOrId) {
	return `hsla(${hashHue(urlOrId).toFixed(0)} 70% 55% / 0.45)`;
}
//#endregion
//#region src/components/player/full.tsx
function FullPlayer() {
	const track = usePlayer((s) => s.queue[s.index]);
	const expanded = usePlayer((s) => s.expanded);
	const isPlaying = usePlayer((s) => s.isPlaying);
	const currentTime = usePlayer((s) => s.currentTime);
	const duration = usePlayer((s) => s.duration);
	const shuffle = usePlayer((s) => s.shuffle);
	const repeat = usePlayer((s) => s.repeat);
	const likedIds = usePlayer((s) => s.likedIds);
	const liked = Boolean(track && likedIds.includes(track.id));
	const lyricsOpen = usePlayer((s) => s.lyricsOpen);
	const toggle = usePlayer((s) => s.toggle);
	const next = usePlayer((s) => s.next);
	const prev = usePlayer((s) => s.prev);
	const seekTo = usePlayer((s) => s.seekTo);
	const toggleShuffle = usePlayer((s) => s.toggleShuffle);
	const cycleRepeat = usePlayer((s) => s.cycleRepeat);
	const toggleLike = usePlayer((s) => s.toggleLike);
	const setExpanded = usePlayer((s) => s.setExpanded);
	const setQueueOpen = usePlayer((s) => s.setQueueOpen);
	const setLyricsOpen = usePlayer((s) => s.setLyricsOpen);
	const [glow, setGlow] = useState("rgba(255,42,61,0.4)");
	useEffect(() => {
		let live = true;
		const url = track?.artworkLg || track?.artwork;
		if (!url) return;
		extractAccent(url).then((c) => {
			if (live && c) setGlow(c);
		});
		return () => {
			live = false;
		};
	}, [track?.artworkLg, track?.artwork]);
	const live = track?.kind === "radio";
	const progress = live || !duration || !Number.isFinite(duration) ? 0 : currentTime / duration * 100;
	return /* @__PURE__ */ jsx("div", {
		"data-open": expanded,
		className: "full-player fixed inset-0 z-40 flex flex-col bg-bg",
		"aria-hidden": !expanded,
		children: track ? /* @__PURE__ */ jsxs(Fragment, { children: [
			/* @__PURE__ */ jsxs("div", {
				className: "pointer-events-none absolute inset-0 overflow-hidden",
				children: [
					track.artworkLg || track.artwork ? /* @__PURE__ */ jsx("img", {
						src: track.artworkLg || track.artwork || "",
						alt: "",
						className: "h-full w-full scale-125 object-cover opacity-35 blur-3xl",
						onError: (e) => {
							e.currentTarget.style.display = "none";
						}
					}) : null,
					/* @__PURE__ */ jsx("div", {
						className: "glow-orb absolute top-1/4 left-1/2 size-[28rem] -translate-x-1/2 rounded-full blur-3xl",
						style: { background: glow }
					}),
					/* @__PURE__ */ jsx("div", { className: "absolute inset-0 bg-gradient-to-b from-bg/20 via-bg/55 to-bg" })
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "relative flex items-center justify-between px-4 py-3",
				children: [
					/* @__PURE__ */ jsx(Button, {
						variant: "icon",
						size: "icon",
						"aria-label": "Close player",
						onClick: () => setExpanded(false),
						children: /* @__PURE__ */ jsx(ChevronDown, { className: "size-6" })
					}),
					/* @__PURE__ */ jsx("p", {
						className: "text-[11px] font-medium tracking-[0.18em] text-muted uppercase",
						children: live ? "Live radio" : "Now playing"
					}),
					/* @__PURE__ */ jsx(Button, {
						variant: "icon",
						size: "icon",
						"aria-label": "Queue",
						onClick: () => setQueueOpen(true),
						children: /* @__PURE__ */ jsx(ListMusic, { className: "size-5" })
					})
				]
			}),
			/* @__PURE__ */ jsx("div", {
				className: "relative flex-1 overflow-y-auto px-4 pb-8 flex flex-col items-center justify-between min-h-0 [scrollbar-width:none]",
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex flex-col items-center justify-center w-full max-w-md my-auto py-2",
					children: [
						/* @__PURE__ */ jsx(Cover, {
							src: track.artworkLg || track.artwork,
							alt: track.title,
							title: track.title,
							rounded: "lg",
							className: cn("aspect-square w-[min(19rem,68vw)] max-h-[36vh] shadow-pop rounded-2xl", isPlaying && "art-playing")
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "mt-5 sm:mt-8 flex w-full items-start justify-between gap-3",
							children: [/* @__PURE__ */ jsxs("div", {
								className: "min-w-0",
								children: [/* @__PURE__ */ jsx("h1", {
									className: "truncate text-xl sm:text-2xl font-bold tracking-tight text-fg",
									children: track.title
								}), /* @__PURE__ */ jsx("p", {
									className: "truncate text-sm sm:text-base text-muted mt-0.5",
									children: track.artist
								})]
							}), /* @__PURE__ */ jsx(Button, {
								variant: "icon",
								size: "icon",
								"aria-label": liked ? "Unlike" : "Like",
								className: "shrink-0",
								onClick: () => toggleLike(track),
								children: /* @__PURE__ */ jsx(Heart, { className: cn("size-6", liked && "fill-accent text-accent") })
							})]
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "mt-4 sm:mt-6 w-full",
							children: [/* @__PURE__ */ jsx("input", {
								className: "player-range",
								type: "range",
								min: 0,
								max: live || !duration ? 0 : duration,
								step: .1,
								value: live ? 0 : currentTime,
								disabled: live,
								style: { "--progress": `${progress}%` },
								onChange: (e) => seekTo(Number(e.target.value)),
								"aria-label": "Seek"
							}), /* @__PURE__ */ jsxs("div", {
								className: "mt-1.5 flex justify-between text-xs tabular-nums text-subtle font-medium",
								children: [/* @__PURE__ */ jsx("span", { children: live ? "LIVE" : formatTime(currentTime) }), /* @__PURE__ */ jsx("span", { children: live ? "∞" : formatTime(Number.isFinite(duration) ? duration : 0) })]
							})]
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "mt-3 sm:mt-5 flex items-center justify-between w-full max-w-xs sm:max-w-sm px-2",
							children: [
								/* @__PURE__ */ jsx(Button, {
									variant: "icon",
									size: "iconSm",
									"aria-label": "Shuffle",
									className: cn("text-muted hover:text-fg", shuffle && "text-accent"),
									onClick: toggleShuffle,
									children: /* @__PURE__ */ jsx(Shuffle, { className: "size-4.5 sm:size-5" })
								}),
								/* @__PURE__ */ jsx(Button, {
									variant: "icon",
									size: "icon",
									"aria-label": "Previous",
									className: "text-fg active:scale-95",
									onClick: prev,
									children: /* @__PURE__ */ jsx(SkipBack, { className: "size-6 sm:size-7 fill-current" })
								}),
								/* @__PURE__ */ jsx(Button, {
									variant: "solid",
									size: "icon",
									"aria-label": isPlaying ? "Pause" : "Play",
									className: "size-14 sm:size-16 rounded-full shadow-lg active:scale-95 transition-transform",
									onClick: toggle,
									children: isPlaying ? /* @__PURE__ */ jsx(Pause, { className: "size-6 sm:size-7 fill-current" }) : /* @__PURE__ */ jsx(Play, {
										className: "size-6 sm:size-7 fill-current",
										style: { marginLeft: 3 }
									})
								}),
								/* @__PURE__ */ jsx(Button, {
									variant: "icon",
									size: "icon",
									"aria-label": "Next",
									className: "text-fg active:scale-95",
									onClick: next,
									children: /* @__PURE__ */ jsx(SkipForward, { className: "size-6 sm:size-7 fill-current" })
								}),
								/* @__PURE__ */ jsx(Button, {
									variant: "icon",
									size: "iconSm",
									"aria-label": "Repeat",
									className: cn("text-muted hover:text-fg", repeat !== "off" && "text-accent"),
									onClick: cycleRepeat,
									children: repeat === "one" ? /* @__PURE__ */ jsx(Repeat1, { className: "size-4.5 sm:size-5" }) : /* @__PURE__ */ jsx(Repeat, { className: "size-4.5 sm:size-5" })
								})
							]
						}),
						/* @__PURE__ */ jsx("div", {
							className: "mt-4 flex items-center gap-2",
							children: /* @__PURE__ */ jsxs(Button, {
								variant: lyricsOpen ? "chip" : "ghost",
								size: "sm",
								className: "rounded-full px-4 text-xs font-medium border border-border/40",
								onClick: () => setLyricsOpen(!lyricsOpen),
								children: [/* @__PURE__ */ jsx(Mic2, { className: "size-3.5 mr-1.5" }), "Lyrics"]
							})
						}),
						/* @__PURE__ */ jsx(LyricsPanel, {}),
						/* @__PURE__ */ jsx("div", {
							className: "mt-4 w-full",
							children: /* @__PURE__ */ jsx(Visualizer, {})
						})
					]
				})
			})
		] }) : null
	});
}
//#endregion
//#region src/components/equalizer.tsx
function Equalizer({ className }) {
	return /* @__PURE__ */ jsxs("span", {
		className: cn("eq", className),
		"aria-hidden": true,
		children: [
			/* @__PURE__ */ jsx("span", {}),
			/* @__PURE__ */ jsx("span", {}),
			/* @__PURE__ */ jsx("span", {})
		]
	});
}
//#endregion
//#region src/components/ui/dropdown-menu.tsx
var DropdownMenu = Dropdown.Root;
var DropdownMenuTrigger = Dropdown.Trigger;
function DropdownMenuContent({ className, ...props }) {
	return /* @__PURE__ */ jsx(Dropdown.Portal, { children: /* @__PURE__ */ jsx(Dropdown.Content, {
		sideOffset: 8,
		className: cn("z-50 min-w-52 overflow-hidden rounded-lg bg-elevated p-1 shadow-pop", "origin-(--radix-dropdown-menu-content-transform-origin)", "data-[state=open]:animate-in data-[state=closed]:animate-out", "data-[state=open]:fade-in-0 data-[state=closed]:fade-out-0", "data-[state=open]:zoom-in-95 data-[state=closed]:zoom-out-95", className),
		...props
	}) });
}
function DropdownMenuItem({ className, ...props }) {
	return /* @__PURE__ */ jsx(Dropdown.Item, {
		className: cn("flex cursor-pointer items-center gap-3 rounded-md px-3 py-2.5 text-sm text-fg outline-none", "data-[highlighted]:bg-hover data-[disabled]:opacity-40", className),
		...props
	});
}
function DropdownMenuSeparator() {
	return /* @__PURE__ */ jsx(Dropdown.Separator, { className: "my-1 h-px bg-border" });
}
//#endregion
//#region src/components/track-menu.tsx
function TrackMenu({ track, rest, children }) {
	const navigate = useNavigate();
	const playNext = usePlayer((s) => s.playNext);
	const addToQueue = usePlayer((s) => s.addToQueue);
	const toggleLike = usePlayer((s) => s.toggleLike);
	const isLiked = usePlayer((s) => s.likedIds).includes(track.id);
	const playTracks = usePlayer((s) => s.playTracks);
	const playlists = usePlayer((s) => s.playlists);
	const addToPlaylist = usePlayer((s) => s.addToPlaylist);
	const createPlaylist = usePlayer((s) => s.createPlaylist);
	return /* @__PURE__ */ jsxs(DropdownMenu, { children: [/* @__PURE__ */ jsx(DropdownMenuTrigger, {
		asChild: true,
		children
	}), /* @__PURE__ */ jsxs(DropdownMenuContent, {
		align: "end",
		children: [
			/* @__PURE__ */ jsxs(DropdownMenuItem, {
				onSelect: () => {
					playNext(track);
					toast("Playing next");
				},
				children: [/* @__PURE__ */ jsx(ListEnd, { className: "size-4 text-muted" }), "Play next"]
			}),
			/* @__PURE__ */ jsxs(DropdownMenuItem, {
				onSelect: () => {
					addToQueue(track);
					toast("Added to queue");
				},
				children: [/* @__PURE__ */ jsx(ListPlus, { className: "size-4 text-muted" }), "Add to queue"]
			}),
			/* @__PURE__ */ jsxs(DropdownMenuItem, {
				onSelect: () => {
					toggleLike(track);
					toast(isLiked ? "Removed from likes" : "Saved to likes");
				},
				children: [/* @__PURE__ */ jsx(Heart, { className: isLiked ? "size-4 fill-accent text-accent" : "size-4 text-muted" }), isLiked ? "Remove from likes" : "Save to likes"]
			}),
			/* @__PURE__ */ jsx(DropdownMenuSeparator, {}),
			playlists.filter((p) => p.id !== "likes").slice(0, 4).map((p) => /* @__PURE__ */ jsxs(DropdownMenuItem, {
				onSelect: () => {
					addToPlaylist(p.id, track);
					toast(`Saved to ${p.name}`);
				},
				children: [
					/* @__PURE__ */ jsx(ListMusic, { className: "size-4 text-muted" }),
					"Add to ",
					p.name
				]
			}, p.id)),
			/* @__PURE__ */ jsxs(DropdownMenuItem, {
				onSelect: () => {
					const name = `Mix · ${track.artist}`;
					createPlaylist(name, [track]);
					toast(`Created ${name}`);
				},
				children: [/* @__PURE__ */ jsx(ListMusic, { className: "size-4 text-muted" }), "New playlist"]
			}),
			/* @__PURE__ */ jsx(DropdownMenuSeparator, {}),
			/* @__PURE__ */ jsxs(DropdownMenuItem, {
				onSelect: async () => {
					try {
						const more = await fetchTrending(24, track.genre);
						playTracks([track, ...more.filter((t) => t.id !== track.id)], 0);
					} catch {
						playTracks(rest?.length ? [track, ...rest] : [track], 0);
					}
				},
				children: [/* @__PURE__ */ jsx(Radio, { className: "size-4 text-muted" }), "Start radio"]
			}),
			track.artistId ? /* @__PURE__ */ jsxs(DropdownMenuItem, {
				onSelect: () => {
					navigate({
						to: "/artist/$id",
						params: { id: track.artistId }
					});
				},
				children: [/* @__PURE__ */ jsx(UserRound, { className: "size-4 text-muted" }), "Go to artist"]
			}) : null
		]
	})] });
}
//#endregion
//#region src/components/track-row.tsx
function TrackRow({ track, index, queue, showPlays }) {
	const current = usePlayer((s) => s.queue[s.index]);
	const isPlaying = usePlayer((s) => s.isPlaying);
	const playTracks = usePlayer((s) => s.playTracks);
	const toggle = usePlayer((s) => s.toggle);
	const active = current?.id === track.id;
	const startIndex = Math.max(0, queue.findIndex((t) => t.id === track.id));
	return /* @__PURE__ */ jsxs("div", {
		className: cn("group grid grid-cols-[auto_minmax(0,1fr)_auto] items-center gap-3 rounded-xl px-2.5 py-2 transition-all duration-150", "hover:bg-white/10 hover:backdrop-blur-md border border-transparent hover:border-white/10", active && "bg-white/15 border-white/15 backdrop-blur-md shadow-sm"),
		children: [
			/* @__PURE__ */ jsxs("button", {
				type: "button",
				className: "relative size-12 shrink-0",
				onClick: () => {
					if (active) toggle();
					else playTracks(queue.length ? queue : [track], startIndex < 0 ? 0 : startIndex);
				},
				children: [
					/* @__PURE__ */ jsx(Cover, {
						src: track.artwork,
						alt: track.title,
						title: track.title,
						className: "size-12"
					}),
					/* @__PURE__ */ jsx("span", {
						className: "absolute inset-0 grid place-items-center rounded-md bg-bg/50 opacity-0 transition-opacity duration-150 group-hover:opacity-100",
						children: active && isPlaying ? /* @__PURE__ */ jsx(Equalizer, {}) : /* @__PURE__ */ jsx(Play, {
							className: "size-5 fill-fg text-fg",
							style: { marginLeft: 2 }
						})
					}),
					active && isPlaying ? /* @__PURE__ */ jsx("span", {
						className: "absolute inset-0 grid place-items-center rounded-md bg-bg/35 group-hover:opacity-0",
						children: /* @__PURE__ */ jsx(Equalizer, {})
					}) : null
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "min-w-0",
				children: [/* @__PURE__ */ jsxs("p", {
					className: cn("truncate text-sm font-medium", active ? "text-accent" : "text-fg"),
					children: [typeof index === "number" ? /* @__PURE__ */ jsx("span", {
						className: "mr-2 tabular-nums text-subtle",
						children: index + 1
					}) : null, track.title]
				}), /* @__PURE__ */ jsxs("p", {
					className: "truncate text-xs text-muted",
					children: [track.artistId ? /* @__PURE__ */ jsx(Link, {
						to: "/artist/$id",
						params: { id: track.artistId },
						className: "hover:text-fg hover:underline",
						children: track.artist
					}) : track.artist, showPlays && track.playCount ? ` · ${formatCount(track.playCount)} plays` : null]
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-center gap-1",
				children: [/* @__PURE__ */ jsx("span", {
					className: "hidden w-10 text-right text-xs tabular-nums text-subtle sm:block",
					children: track.kind === "radio" ? "LIVE" : formatTime(track.duration)
				}), /* @__PURE__ */ jsx(TrackMenu, {
					track,
					rest: queue,
					children: /* @__PURE__ */ jsx(Button, {
						variant: "icon",
						size: "iconSm",
						"aria-label": "More",
						children: /* @__PURE__ */ jsx(MoreVertical, { className: "size-4" })
					})
				})]
			})
		]
	});
}
//#endregion
//#region src/components/player/queue.tsx
function QueuePanel() {
	const open = usePlayer((s) => s.queueOpen);
	const queue = usePlayer((s) => s.queue);
	const index = usePlayer((s) => s.index);
	const setQueueOpen = usePlayer((s) => s.setQueueOpen);
	const upcoming = queue.slice(index + 1);
	const current = queue[index];
	return /* @__PURE__ */ jsxs("aside", {
		"data-open": open,
		className: "queue-drawer fixed top-0 right-0 z-50 flex h-[calc(100dvh-var(--spacing-player))] w-[min(22rem,100vw)] flex-col border-l border-border bg-surface",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "flex items-center justify-between px-4 py-3",
			children: [/* @__PURE__ */ jsx("h2", {
				className: "text-sm font-semibold",
				children: "Queue"
			}), /* @__PURE__ */ jsx(Button, {
				variant: "icon",
				size: "iconSm",
				"aria-label": "Close queue",
				onClick: () => setQueueOpen(false),
				children: /* @__PURE__ */ jsx(X, { className: "size-4" })
			})]
		}), /* @__PURE__ */ jsxs("div", {
			className: "flex-1 overflow-y-auto px-2 pb-4",
			children: [current ? /* @__PURE__ */ jsxs("div", {
				className: "mb-4",
				children: [/* @__PURE__ */ jsx("p", {
					className: "px-2 pb-2 text-[11px] font-medium tracking-wide text-subtle uppercase",
					children: "Now playing"
				}), /* @__PURE__ */ jsx(TrackRow, {
					track: current,
					queue
				})]
			}) : /* @__PURE__ */ jsx("p", {
				className: "px-3 py-8 text-sm text-muted",
				children: "Nothing in the queue yet."
			}), upcoming.length ? /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
				className: "px-2 pb-2 text-[11px] font-medium tracking-wide text-subtle uppercase",
				children: "Next"
			}), upcoming.map((t) => /* @__PURE__ */ jsx(TrackRow, {
				track: t,
				queue
			}, t.id))] }) : null]
		})]
	});
}
//#endregion
//#region src/components/ui/dialog.tsx
var Dialog = D.Root;
var DialogTrigger = D.Trigger;
function DialogContent({ title, children, className }) {
	return /* @__PURE__ */ jsxs(D.Portal, { children: [/* @__PURE__ */ jsx(D.Overlay, { className: "fixed inset-0 z-50 bg-bg/70 data-[state=open]:animate-in data-[state=open]:fade-in-0 data-[state=closed]:animate-out data-[state=closed]:fade-out-0" }), /* @__PURE__ */ jsxs(D.Content, {
		className: cn("fixed top-1/2 left-1/2 z-50 w-[min(28rem,calc(100vw-2rem))] -translate-x-1/2 -translate-y-1/2", "rounded-xl bg-elevated p-5 shadow-pop", "data-[state=open]:animate-in data-[state=open]:fade-in-0 data-[state=open]:zoom-in-95", className),
		children: [/* @__PURE__ */ jsxs("div", {
			className: "mb-4 flex items-center justify-between gap-3",
			children: [/* @__PURE__ */ jsx(D.Title, {
				className: "text-lg font-semibold tracking-tight",
				children: title
			}), /* @__PURE__ */ jsx(D.Close, {
				className: "grid size-8 place-items-center rounded-full text-muted hover:bg-hover hover:text-fg",
				children: /* @__PURE__ */ jsx(X, { className: "size-4" })
			})]
		}), children]
	})] });
}
/**
* Current user + loading state. Same behavior in live preview and when deployed:
*   - Auth enabled -> the real signed-in user; `user` is `null` while
*                            the session resolves (`isPending: true`) and when
*                            signed out (`isPending: false`). Session comes from
*                            Better Auth `useSession()` → `/api/auth/get-session`
*                            (cookie when deployed; bearer in live preview).
*   - Auth disabled (`VITE_AUTH_ENABLED=false`) -> `DEV_USER`, never pending.
*
* Protect a route by waiting out `isPending` before acting on `user` —
* redirecting on `user: null` alone bounces signed-in visitors to sign-in on
* every hard reload:
*
*   import { RedirectToSignIn } from "@/lib/auth/gates";
*   const { user, isPending } = useCurrentUserState();
*   if (isPending) return null;              // still resolving — don't redirect yet
*   if (!user) return <RedirectToSignIn />;  // definitely signed out
*
* `authEnabled` is a module-level constant fixed at load, so the guarded hook
* call keeps a stable hook order across every render of a given component.
*/
function useCurrentUserState() {
	const { data, isPending } = authClient.useSession();
	const user = data?.user;
	return {
		user: user ? {
			id: user.id,
			displayName: user.name ?? null,
			primaryEmail: user.email ?? null,
			profileImageUrl: user.image ?? null,
			isDevFallback: false
		} : null,
		isPending
	};
}
//#endregion
//#region src/components/app-shell.tsx
var NAV = [
	{
		to: "/",
		label: "Home",
		icon: Home
	},
	{
		to: "/explore",
		label: "Explore",
		icon: Compass
	},
	{
		to: "/studio",
		label: "Studio",
		icon: Disc
	},
	{
		to: "/radio",
		label: "Radio",
		icon: Radio
	},
	{
		to: "/library",
		label: "Library",
		icon: Library
	}
];
function AppShell({ children }) {
	const path = useRouterState({ select: (s) => s.location.pathname });
	const navigate = useNavigate();
	const { user } = useCurrentUserState();
	const hasTrack = usePlayer((s) => Boolean(s.queue[s.index]));
	const rawPlaylists = usePlayer((s) => s.playlists);
	const playlists = useMemo(() => rawPlaylists.filter((p) => p.id !== "likes"), [rawPlaylists]);
	const createPlaylist = usePlayer((s) => s.createPlaylist);
	const rememberSearch = usePlayer((s) => s.rememberSearch);
	const [q, setQ] = useState("");
	const [openCreate, setOpenCreate] = useState(false);
	const [name, setName] = useState("");
	const [mobileSearchOpen, setMobileSearchOpen] = useState(false);
	const onSearch = (e) => {
		e.preventDefault();
		const query = q.trim();
		if (!query) return;
		rememberSearch(query);
		navigate({
			to: "/search",
			search: { q: query }
		});
	};
	return /* @__PURE__ */ jsxs("div", {
		className: "min-h-dvh bg-transparent text-fg relative isolate selection:bg-accent/40 selection:text-white",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "fixed inset-0 pointer-events-none -z-10 overflow-hidden select-none",
				"aria-hidden": "true",
				children: [
					/* @__PURE__ */ jsx("video", {
						className: "absolute inset-0 h-full w-full object-cover object-center opacity-90 scale-100 filter brightness-105 contrast-110 saturate-135",
						autoPlay: true,
						muted: true,
						loop: true,
						playsInline: true,
						disablePictureInPicture: true,
						children: /* @__PURE__ */ jsx("source", {
							src: "https://d8j0ntlcm91z4.cloudfront.net/user_38xzZboKViGWJOttwIXH07lWA1P/hf_20260808_064556_051587f1-74a1-4336-8c05-4dde3594ed05.mp4",
							type: "video/mp4"
						})
					}),
					/* @__PURE__ */ jsx("div", { className: "absolute inset-0 bg-gradient-to-b from-black/60 via-black/10 to-black/75" }),
					/* @__PURE__ */ jsx("div", { className: "absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-transparent via-black/15 to-black/60" })
				]
			}),
			/* @__PURE__ */ jsx(PlayerEngine, {}),
			/* @__PURE__ */ jsx(Toaster, {
				theme: "dark",
				position: "bottom-center",
				offset: hasTrack ? 88 : 24,
				toastOptions: { className: "bg-black/80 backdrop-blur-2xl text-fg border-white/15 shadow-2xl" }
			}),
			/* @__PURE__ */ jsxs("aside", {
				className: "fixed top-0 left-0 z-20 hidden h-dvh w-sidebar flex-col bg-black/40 backdrop-blur-3xl border-r border-white/10 px-4 pt-5 pb-player md:flex",
				children: [
					/* @__PURE__ */ jsx(Logo, {}),
					/* @__PURE__ */ jsx("nav", {
						className: "mt-8 flex flex-col gap-1",
						children: NAV.map((item) => {
							const active = item.to === "/" ? path === "/" : path.startsWith(item.to);
							const Icon = item.icon;
							return /* @__PURE__ */ jsxs(Link, {
								to: item.to,
								className: cn("flex h-11 items-center gap-4 rounded-xl px-3.5 text-sm font-medium transition-all duration-150", active ? "bg-white/15 text-fg shadow-sm border border-white/20 backdrop-blur-md" : "text-muted hover:bg-white/10 hover:text-fg"),
								children: [/* @__PURE__ */ jsx(Icon, {
									className: "size-5",
									strokeWidth: active ? 2.4 : 2
								}), item.label]
							}, item.to);
						})
					}),
					/* @__PURE__ */ jsx("div", {
						className: "mt-6",
						children: /* @__PURE__ */ jsxs(Dialog, {
							open: openCreate,
							onOpenChange: setOpenCreate,
							children: [/* @__PURE__ */ jsx(DialogTrigger, {
								asChild: true,
								children: /* @__PURE__ */ jsxs(Button, {
									variant: "ghost",
									className: "w-full justify-start gap-4 rounded-xl px-3.5 text-muted hover:text-fg hover:bg-white/10",
									children: [/* @__PURE__ */ jsx("span", {
										className: "grid size-6 place-items-center rounded-md bg-white/10 border border-white/15",
										children: /* @__PURE__ */ jsx(Plus, { className: "size-4" })
									}), "New playlist"]
								})
							}), /* @__PURE__ */ jsx(DialogContent, {
								title: "New playlist",
								children: /* @__PURE__ */ jsxs("form", {
									className: "space-y-4",
									onSubmit: (e) => {
										e.preventDefault();
										createPlaylist(name);
										setName("");
										setOpenCreate(false);
										navigate({ to: "/library" });
									},
									children: [/* @__PURE__ */ jsx("input", {
										autoFocus: true,
										value: name,
										onChange: (e) => setName(e.target.value),
										placeholder: "Playlist name",
										className: "h-11 w-full rounded-xl bg-black/60 px-3.5 text-sm outline-none ring-accent/50 focus:ring-2 border border-white/20"
									}), /* @__PURE__ */ jsx(Button, {
										variant: "solid",
										className: "w-full rounded-xl",
										type: "submit",
										children: "Create"
									})]
								})
							})]
						})
					}),
					/* @__PURE__ */ jsx("div", {
						className: "mt-4 flex-1 overflow-y-auto [scrollbar-width:none]",
						children: playlists.map((p) => /* @__PURE__ */ jsx(Link, {
							to: "/library",
							className: "block truncate rounded-lg px-3 py-2 text-sm text-muted hover:text-fg hover:bg-white/10 transition-colors",
							children: p.name
						}, p.id))
					}),
					/* @__PURE__ */ jsx("div", {
						className: "mt-auto pt-4 border-t border-white/10",
						children: user && !user.isDevFallback ? /* @__PURE__ */ jsxs("div", {
							className: "flex items-center justify-between gap-2 p-2 rounded-2xl bg-white/5 border border-white/10",
							children: [/* @__PURE__ */ jsxs(Link, {
								to: "/studio",
								className: "flex items-center gap-2.5 min-w-0 flex-1 hover:opacity-80 transition-opacity",
								children: [/* @__PURE__ */ jsx(Cover, {
									src: user.profileImageUrl,
									alt: user.displayName || "User",
									title: user.displayName || "User",
									rounded: "full",
									className: "size-8 shrink-0"
								}), /* @__PURE__ */ jsxs("div", {
									className: "min-w-0 flex-1",
									children: [/* @__PURE__ */ jsx("p", {
										className: "text-xs font-semibold text-fg truncate",
										children: user.displayName || "Artist"
									}), /* @__PURE__ */ jsx("p", {
										className: "text-[10px] text-muted truncate",
										children: "Artist Studio"
									})]
								})]
							}), /* @__PURE__ */ jsx("button", {
								type: "button",
								onClick: () => {
									signOut().then(() => toast.success("Signed out"));
								},
								className: "p-1.5 text-muted hover:text-red-400 transition-colors rounded-lg hover:bg-white/10",
								title: "Sign out",
								children: /* @__PURE__ */ jsx(LogOut, { className: "size-4" })
							})]
						}) : /* @__PURE__ */ jsxs(Link, {
							to: "/login",
							className: "flex h-10 w-full items-center justify-center gap-2 rounded-xl bg-accent/20 hover:bg-accent/30 text-accent font-medium text-xs border border-accent/30 transition-all shadow-sm",
							children: [/* @__PURE__ */ jsx(User, { className: "size-3.5" }), "Sign In / Artist Join"]
						})
					})
				]
			}),
			/* @__PURE__ */ jsx("header", {
				className: "sticky top-0 z-20 flex flex-col justify-center bg-black/45 backdrop-blur-3xl border-b border-white/10 px-4 md:px-8 transition-all md:ml-sidebar pt-[env(safe-area-inset-top,0px)] shadow-lg",
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex h-16 md:h-18 items-center justify-between gap-3 sm:gap-6 w-full",
					children: [mobileSearchOpen ? /* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-2 w-full animate-in fade-in duration-150 md:hidden",
						children: [/* @__PURE__ */ jsxs("form", {
							onSubmit: (e) => {
								onSearch(e);
								setMobileSearchOpen(false);
							},
							className: "relative flex-1",
							children: [
								/* @__PURE__ */ jsx(Search, { className: "pointer-events-none absolute top-1/2 left-3.5 size-4 -translate-y-1/2 text-muted" }),
								/* @__PURE__ */ jsx("input", {
									autoFocus: true,
									value: q,
									onChange: (e) => setQ(e.target.value),
									placeholder: "Search songs, artists, radio...",
									className: "h-10 w-full rounded-full bg-black/80 pr-9 pl-10 text-xs text-fg outline-none ring-accent/50 focus:ring-2 border border-white/20 backdrop-blur-2xl shadow-inner"
								}),
								q ? /* @__PURE__ */ jsx("button", {
									type: "button",
									onClick: () => setQ(""),
									className: "absolute top-1/2 right-3 -translate-y-1/2 text-muted hover:text-fg",
									children: /* @__PURE__ */ jsx(X, { className: "size-3.5" })
								}) : null
							]
						}), /* @__PURE__ */ jsx("button", {
							type: "button",
							onClick: () => setMobileSearchOpen(false),
							className: "px-2.5 py-1.5 text-xs font-semibold text-muted hover:text-fg active:scale-95 transition-colors",
							children: "Cancel"
						})]
					}) : /* @__PURE__ */ jsxs("div", {
						className: "flex items-center justify-between w-full md:hidden",
						children: [/* @__PURE__ */ jsx(Logo, { compact: false }), /* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-2",
							children: [user && !user.isDevFallback ? /* @__PURE__ */ jsx(Link, {
								to: "/studio",
								className: "grid size-9 place-items-center rounded-full overflow-hidden border border-white/20 bg-white/10",
								title: "Artist Studio",
								children: /* @__PURE__ */ jsx(Cover, {
									src: user.profileImageUrl,
									alt: user.displayName || "User",
									title: user.displayName || "User",
									rounded: "full",
									className: "size-9"
								})
							}) : /* @__PURE__ */ jsx(Link, {
								to: "/login",
								className: "px-3 py-1.5 rounded-full bg-accent/20 hover:bg-accent/30 text-accent border border-accent/30 text-[11px] font-semibold transition-all",
								children: "Sign In"
							}), /* @__PURE__ */ jsx("button", {
								type: "button",
								"aria-label": "Open search",
								onClick: () => setMobileSearchOpen(true),
								className: "grid size-9 place-items-center rounded-full bg-white/10 hover:bg-white/15 active:scale-95 text-fg border border-white/15 backdrop-blur-xl transition-all shadow-sm",
								children: /* @__PURE__ */ jsx(Search, { className: "size-4" })
							})]
						})]
					}), /* @__PURE__ */ jsxs("div", {
						className: "hidden md:flex items-center justify-between gap-4 flex-1",
						children: [/* @__PURE__ */ jsxs("form", {
							onSubmit: onSearch,
							className: "relative flex-1 max-w-xl",
							children: [/* @__PURE__ */ jsx(Search, { className: "pointer-events-none absolute top-1/2 left-3.5 size-4 -translate-y-1/2 text-muted/70 transition-colors" }), /* @__PURE__ */ jsx("input", {
								value: q,
								onChange: (e) => setQ(e.target.value),
								placeholder: "Search songs, artists, radio...",
								className: "h-11 w-full rounded-full bg-white/10 hover:bg-white/15 focus:bg-black/80 pr-4 pl-10 text-sm text-fg outline-none ring-accent/50 placeholder:text-white/45 focus:ring-2 border border-white/15 backdrop-blur-2xl transition-all shadow-inner"
							})]
						}), /* @__PURE__ */ jsx("div", {
							className: "flex items-center gap-3",
							children: user && !user.isDevFallback ? /* @__PURE__ */ jsxs(Link, {
								to: "/studio",
								className: "flex items-center gap-2 px-3.5 py-1.5 rounded-full bg-white/10 hover:bg-white/15 text-xs font-medium text-fg border border-white/15 backdrop-blur-xl transition-all",
								children: [/* @__PURE__ */ jsx(Cover, {
									src: user.profileImageUrl,
									alt: user.displayName || "User",
									title: user.displayName || "User",
									rounded: "full",
									className: "size-6"
								}), /* @__PURE__ */ jsx("span", {
									className: "max-w-28 truncate",
									children: user.displayName || "Studio"
								})]
							}) : /* @__PURE__ */ jsxs(Link, {
								to: "/login",
								className: "flex items-center gap-1.5 px-4 py-2 rounded-full bg-accent hover:bg-accent/90 text-white text-xs font-semibold shadow-md shadow-accent/20 transition-all",
								children: [/* @__PURE__ */ jsx(User, { className: "size-3.5" }), "Sign In"]
							})
						})]
					})]
				})
			}),
			/* @__PURE__ */ jsx("main", {
				className: cn("md:ml-sidebar transition-all", hasTrack ? "pb-[calc(var(--spacing-player)+var(--spacing-nav)+1rem)] md:pb-player" : "pb-[calc(var(--spacing-nav)+1rem)] md:pb-8"),
				children
			}),
			/* @__PURE__ */ jsxs("div", {
				className: cn("fixed inset-x-0 bottom-0 z-30", hasTrack ? "" : "md:hidden"),
				children: [/* @__PURE__ */ jsx(PlayerBar, {}), /* @__PURE__ */ jsx("nav", {
					className: "flex h-nav items-center justify-around border-t border-white/10 bg-black/60 backdrop-blur-3xl pb-[env(safe-area-inset-bottom)] md:hidden",
					children: NAV.map((item) => {
						const active = item.to === "/" ? path === "/" : path.startsWith(item.to);
						const Icon = item.icon;
						return /* @__PURE__ */ jsxs(Link, {
							to: item.to,
							className: cn("flex min-w-16 flex-col items-center justify-center gap-0.5 py-1 text-[10px] font-medium transition-all active:scale-95", active ? "text-fg" : "text-muted hover:text-fg"),
							children: [/* @__PURE__ */ jsx("span", {
								className: cn("grid size-8 place-items-center rounded-full transition-colors", active && "bg-white/15 text-accent border border-white/20 backdrop-blur-md"),
								children: /* @__PURE__ */ jsx(Icon, {
									className: "size-4.5",
									strokeWidth: active ? 2.5 : 2
								})
							}), /* @__PURE__ */ jsx("span", {
								className: cn(active ? "font-semibold text-fg" : "text-subtle"),
								children: item.label
							})]
						}, item.to);
					})
				})]
			}),
			/* @__PURE__ */ jsx(FullPlayer, {}),
			/* @__PURE__ */ jsx(QueuePanel, {})
		]
	});
}
//#endregion
//#region src/lib/query-provider.tsx
function QueryProvider({ children }) {
	const [client] = useState(() => new QueryClient({ defaultOptions: { queries: {
		staleTime: 3e5,
		retry: 1,
		refetchOnWindowFocus: false
	} } }));
	return /* @__PURE__ */ jsx(QueryClientProvider, {
		client,
		children
	});
}
//#endregion
//#region src/styles.css?url
var styles_default = "/assets/styles-BAqzZyuF.css";
//#endregion
//#region src/routes/__root.tsx
var APP_NAME = "Sonara Music";
var Route$12 = createRootRoute({
	head: () => ({
		meta: [
			{ charSet: "utf-8" },
			{
				name: "viewport",
				content: "width=device-width, initial-scale=1, viewport-fit=cover"
			},
			{ title: APP_NAME },
			{
				name: "description",
				content: "Stream independent music and live radio with Sonara dark music player."
			},
			{
				name: "theme-color",
				content: "#030303"
			}
		],
		links: [
			{
				rel: "icon",
				type: "image/svg+xml",
				href: "/favicon.svg"
			},
			{
				rel: "stylesheet",
				href: styles_default
			},
			{
				rel: "manifest",
				href: "/manifest.webmanifest"
			},
			{
				rel: "apple-touch-icon",
				href: "/favicon.svg"
			},
			{
				rel: "preconnect",
				href: "https://fonts.googleapis.com"
			},
			{
				rel: "preconnect",
				href: "https://fonts.gstatic.com",
				crossOrigin: "anonymous"
			},
			{
				rel: "stylesheet",
				href: "https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap"
			}
		]
	}),
	component: () => /* @__PURE__ */ jsxs("html", {
		lang: "en",
		className: "dark antialiased",
		suppressHydrationWarning: true,
		children: [/* @__PURE__ */ jsx("head", { children: /* @__PURE__ */ jsx(HeadContent, {}) }), /* @__PURE__ */ jsxs("body", {
			className: "bg-bg text-fg",
			children: [
				/* @__PURE__ */ jsx(PreviewHostBridge, {}),
				/* @__PURE__ */ jsx(AuthProvider, { children: /* @__PURE__ */ jsx(QueryProvider, { children: /* @__PURE__ */ jsx(AppShell, { children: /* @__PURE__ */ jsx(Outlet, {}) }) }) }),
				/* @__PURE__ */ jsx(Scripts, {})
			]
		})]
	})
});
//#endregion
//#region src/routes/index.tsx
var $$splitComponentImporter$9 = () => import("./routes-Cv_QfgSm.js");
var Route$11 = createFileRoute("/")({ component: lazyRouteComponent($$splitComponentImporter$9, "component") });
//#endregion
//#region src/routes/explore.tsx
var $$splitComponentImporter$8 = () => import("./explore-D9RPEcL1.js");
var Route$10 = createFileRoute("/explore")({ component: lazyRouteComponent($$splitComponentImporter$8, "component") });
//#endregion
//#region src/routes/library.tsx
var $$splitComponentImporter$7 = () => import("./library-XC1plz1D.js");
var Route$9 = createFileRoute("/library")({ component: lazyRouteComponent($$splitComponentImporter$7, "component") });
//#endregion
//#region src/routes/login.tsx
var $$splitComponentImporter$6 = () => import("./login-CKeZNfBN.js");
var Route$8 = createFileRoute("/login")({ component: lazyRouteComponent($$splitComponentImporter$6, "component") });
//#endregion
//#region src/routes/radio.tsx
var $$splitComponentImporter$5 = () => import("./radio-J-Hn5WZb.js");
var Route$7 = createFileRoute("/radio")({ component: lazyRouteComponent($$splitComponentImporter$5, "component") });
//#endregion
//#region src/routes/search.tsx
var $$splitComponentImporter$4 = () => import("./search-6qZQw81l.js");
var Route$6 = createFileRoute("/search")({
	validateSearch: (s) => ({ q: typeof s.q === "string" ? s.q : "" }),
	component: lazyRouteComponent($$splitComponentImporter$4, "component")
});
//#endregion
//#region src/routes/studio.tsx
var $$splitComponentImporter$3 = () => import("./studio-QcgGYK1W.js");
var Route$5 = createFileRoute("/studio")({ component: lazyRouteComponent($$splitComponentImporter$3, "component") });
//#endregion
//#region src/routes/artist.$id.tsx
var $$splitComponentImporter$2 = () => import("./artist._id-DNkipVJK.js");
var Route$4 = createFileRoute("/artist/$id")({ component: lazyRouteComponent($$splitComponentImporter$2, "component") });
//#endregion
//#region src/routes/genre.$slug.tsx
var $$splitComponentImporter$1 = () => import("./genre._slug-AxlL5KYM.js");
var Route$3 = createFileRoute("/genre/$slug")({ component: lazyRouteComponent($$splitComponentImporter$1, "component") });
//#endregion
//#region src/routes/playlist.$id.tsx
var $$splitComponentImporter = () => import("./playlist._id-jhHurjc_.js");
var Route$2 = createFileRoute("/playlist/$id")({ component: lazyRouteComponent($$splitComponentImporter, "component") });
//#endregion
//#region src/routes/api/auth/$.ts
var Route$1 = createFileRoute("/api/auth/$")({ server: { handlers: {
	GET: ({ request }) => auth.handler(request),
	POST: ({ request }) => auth.handler(request)
} } });
//#endregion
//#region src/routes/api/storage/upload.ts
var ALLOWED_AUDIO = /* @__PURE__ */ new Set([
	"audio/mpeg",
	"audio/mp3",
	"audio/wav",
	"audio/x-wav",
	"audio/aac",
	"audio/mp4",
	"audio/x-m4a",
	"audio/ogg",
	"audio/flac"
]);
var ALLOWED_IMAGE = /* @__PURE__ */ new Set([
	"image/jpeg",
	"image/png",
	"image/webp",
	"image/avif"
]);
var MAX_AUDIO_SIZE = 62914560;
var MAX_IMAGE_SIZE = 10485760;
var Route = createFileRoute("/api/storage/upload")({ server: { handlers: { POST: async ({ request }) => {
	try {
		const formData = await request.formData();
		const file = formData.get("file");
		const category = formData.get("category") || "audio";
		if (!file || typeof file === "string") return new Response(JSON.stringify({ error: "No file provided" }), {
			status: 400,
			headers: { "Content-Type": "application/json" }
		});
		const mimeType = file.type || (category === "audio" ? "audio/mpeg" : "image/jpeg");
		const fileSize = file.size;
		if (category === "audio") {
			if (!ALLOWED_AUDIO.has(mimeType) && !file.name.match(/\.(mp3|wav|aac|m4a|ogg|flac)$/i)) return new Response(JSON.stringify({ error: "Invalid audio format. Allowed: MP3, WAV, AAC, M4A, OGG, FLAC." }), {
				status: 400,
				headers: { "Content-Type": "application/json" }
			});
			if (fileSize > MAX_AUDIO_SIZE) return new Response(JSON.stringify({ error: "Audio file too large. Maximum size is 60MB." }), {
				status: 400,
				headers: { "Content-Type": "application/json" }
			});
		} else {
			if (!ALLOWED_IMAGE.has(mimeType) && !file.name.match(/\.(jpe?g|png|webp|avif)$/i)) return new Response(JSON.stringify({ error: "Invalid image format. Allowed: JPG, PNG, WebP." }), {
				status: 400,
				headers: { "Content-Type": "application/json" }
			});
			if (fileSize > MAX_IMAGE_SIZE) return new Response(JSON.stringify({ error: "Artwork file too large. Maximum size is 10MB." }), {
				status: 400,
				headers: { "Content-Type": "application/json" }
			});
		}
		const ext = file.name.split(".").pop()?.toLowerCase() || (category === "audio" ? "mp3" : "jpg");
		const filename = `${randomUUID()}.${ext}`;
		const subDir = category === "audio" ? "audio" : "artwork";
		const targetDir = join(process.cwd(), "public", "uploads", subDir);
		await mkdir(targetDir, { recursive: true });
		const filePath = join(targetDir, filename);
		const buffer = Buffer.from(await file.arrayBuffer());
		await writeFile(filePath, buffer);
		const publicUrl = `/uploads/${subDir}/${filename}`;
		const storageKey = `${subDir}/${filename}`;
		return new Response(JSON.stringify({
			success: true,
			url: publicUrl,
			storageKey,
			mimeType,
			fileSize,
			name: file.name
		}), {
			status: 200,
			headers: { "Content-Type": "application/json" }
		});
	} catch (err) {
		const message = err instanceof Error ? err.message : "Upload failed";
		return new Response(JSON.stringify({ error: message }), {
			status: 500,
			headers: { "Content-Type": "application/json" }
		});
	}
} } } });
//#endregion
//#region src/routeTree.gen.ts
var rootRouteChildren = {
	IndexRoute: Route$11.update({
		id: "/",
		path: "/",
		getParentRoute: () => Route$12
	}),
	ExploreRoute: Route$10.update({
		id: "/explore",
		path: "/explore",
		getParentRoute: () => Route$12
	}),
	LibraryRoute: Route$9.update({
		id: "/library",
		path: "/library",
		getParentRoute: () => Route$12
	}),
	LoginRoute: Route$8.update({
		id: "/login",
		path: "/login",
		getParentRoute: () => Route$12
	}),
	RadioRoute: Route$7.update({
		id: "/radio",
		path: "/radio",
		getParentRoute: () => Route$12
	}),
	SearchRoute: Route$6.update({
		id: "/search",
		path: "/search",
		getParentRoute: () => Route$12
	}),
	StudioRoute: Route$5.update({
		id: "/studio",
		path: "/studio",
		getParentRoute: () => Route$12
	}),
	ArtistIdRoute: Route$4.update({
		id: "/artist/$id",
		path: "/artist/$id",
		getParentRoute: () => Route$12
	}),
	GenreSlugRoute: Route$3.update({
		id: "/genre/$slug",
		path: "/genre/$slug",
		getParentRoute: () => Route$12
	}),
	PlaylistIdRoute: Route$2.update({
		id: "/playlist/$id",
		path: "/playlist/$id",
		getParentRoute: () => Route$12
	}),
	ApiAuthSplatRoute: Route$1.update({
		id: "/api/auth/$",
		path: "/api/auth/$",
		getParentRoute: () => Route$12
	}),
	ApiStorageUploadRoute: Route.update({
		id: "/api/storage/upload",
		path: "/api/storage/upload",
		getParentRoute: () => Route$12
	})
};
var routeTree = Route$12._addFileChildren(rootRouteChildren)._addFileTypes();
//#endregion
//#region src/router.tsx
var router_exports = /* @__PURE__ */ __exportAll({ getRouter: () => getRouter });
function getRouter() {
	return createRouter({
		routeTree,
		defaultErrorComponent: AppErrorComponent
	});
}
//#endregion
export { getPublicArtistProfileServerFn as A, searchTracks as C, getCommunityReleasesServerFn as D, deleteArtistTrackServerFn as E, publishTrackServerFn as M, updateArtistProfileServerFn as N, getMyArtistTracksServerFn as O, Logo as P, searchRadio as S, formatDurationTotal as T, fetchTrendingPlaylists as _, Route$6 as a, searchArtists as b, DialogContent as c, Equalizer as d, fetchArtist as f, fetchTrending as g, getRouter, fetchRadioStations as h, Route$4 as i, getPublicArtistTracksServerFn as j, getOrCreateArtistProfileServerFn as k, DialogTrigger as l, fetchPlaylist as m, Route$2 as n, useCurrentUserState as o, fetchArtistTracks as p, Route$3 as r, Dialog as s, router_exports as t, TrackRow as u, fetchUnderground as v, formatCount as w, searchPlaylists as x, radioToTrack as y };
