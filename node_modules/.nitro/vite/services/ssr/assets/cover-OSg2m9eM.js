import { n as cn, r as hashHue } from "./button-D6cJemEt.js";
import { useEffect, useState } from "react";
import { jsx } from "react/jsx-runtime";
import { create } from "zustand";
import { createJSONStorage, persist } from "zustand/middleware";
//#region src/lib/player-store.ts
var MAX_RECENTS = 40;
function remember(list, track) {
	return [track, ...list.filter((t) => t.id !== track.id)].slice(0, MAX_RECENTS);
}
function nextIndex(state) {
	const { queue, index, repeat, shuffle } = state;
	if (queue.length === 0) return null;
	if (repeat === "one") return index;
	if (shuffle && queue.length > 1) {
		let n = index;
		while (n === index) n = Math.floor(Math.random() * queue.length);
		return n;
	}
	if (index < queue.length - 1) return index + 1;
	if (repeat === "all") return 0;
	return null;
}
var usePlayer = create()(persist((set, get) => ({
	queue: [],
	index: 0,
	isPlaying: false,
	currentTime: 0,
	duration: 0,
	pendingSeek: null,
	volume: .9,
	muted: false,
	shuffle: false,
	repeat: "off",
	expanded: false,
	queueOpen: false,
	lyricsOpen: false,
	likedIds: [],
	recents: [],
	playlists: [],
	recentSearches: [],
	hydrated: false,
	current: () => {
		const s = get();
		return s.queue[s.index] ?? null;
	},
	isLiked: (id) => get().likedIds.includes(id),
	playTracks: (tracks, startIndex = 0) => {
		const clean = tracks.filter(Boolean);
		if (!clean.length) return;
		const i = Math.min(Math.max(0, startIndex), clean.length - 1);
		const start = clean[i];
		if (!start) return;
		set({
			queue: clean,
			index: i,
			isPlaying: true,
			currentTime: 0,
			recents: remember(get().recents, start)
		});
	},
	playTrack: (track, rest = []) => {
		const queue = [track, ...rest.filter((t) => t.id !== track.id)];
		get().playTracks(queue, 0);
	},
	toggle: () => {
		const s = get();
		if (!s.queue.length) return;
		set({ isPlaying: !s.isPlaying });
	},
	setPlaying: (v) => set({ isPlaying: v }),
	next: () => {
		const s = get();
		const n = nextIndex(s);
		if (n == null) {
			set({ isPlaying: false });
			return;
		}
		const track = s.queue[n];
		set({
			index: n,
			currentTime: 0,
			isPlaying: true,
			recents: track ? remember(s.recents, track) : s.recents
		});
	},
	prev: () => {
		const s = get();
		if (s.currentTime > 3) {
			set({ currentTime: 0 });
			return;
		}
		if (s.index > 0) {
			const n = s.index - 1;
			const track = s.queue[n];
			set({
				index: n,
				currentTime: 0,
				isPlaying: true,
				recents: track ? remember(s.recents, track) : s.recents
			});
		} else set({ currentTime: 0 });
	},
	seekTo: (t) => set({
		currentTime: Math.max(0, t),
		pendingSeek: Math.max(0, t)
	}),
	setCurrentTime: (t) => set({ currentTime: t }),
	clearPendingSeek: () => set({ pendingSeek: null }),
	setDuration: (t) => set({ duration: t }),
	setVolume: (v) => set({
		volume: Math.min(1, Math.max(0, v)),
		muted: v === 0
	}),
	toggleMute: () => set({ muted: !get().muted }),
	toggleShuffle: () => set({ shuffle: !get().shuffle }),
	cycleRepeat: () => {
		const order = [
			"off",
			"all",
			"one"
		];
		set({ repeat: order[(order.indexOf(get().repeat) + 1) % order.length] ?? "off" });
	},
	toggleLike: (track) => {
		const liked = get().likedIds.includes(track.id);
		set({
			likedIds: liked ? get().likedIds.filter((id) => id !== track.id) : [track.id, ...get().likedIds],
			recents: remember(get().recents, track)
		});
		if (!liked) {
			if (!get().playlists.find((p) => p.id === "likes")) set({ playlists: [{
				id: "likes",
				name: "Liked songs",
				tracks: [track],
				createdAt: Date.now()
			}, ...get().playlists] });
			else get().addToPlaylist("likes", track);
		} else get().removeFromPlaylist("likes", track.id);
	},
	addToQueue: (track) => {
		const s = get();
		if (!s.queue.length) {
			get().playTracks([track], 0);
			return;
		}
		set({ queue: [...s.queue, track] });
	},
	playNext: (track) => {
		const s = get();
		if (!s.queue.length) {
			get().playTracks([track], 0);
			return;
		}
		const queue = [...s.queue];
		queue.splice(s.index + 1, 0, track);
		set({ queue });
	},
	removeFromQueue: (i) => {
		const s = get();
		if (i < 0 || i >= s.queue.length) return;
		const queue = s.queue.filter((_, idx) => idx !== i);
		let index = s.index;
		if (i < s.index) index -= 1;
		if (i === s.index) index = Math.min(index, Math.max(0, queue.length - 1));
		set({
			queue,
			index,
			isPlaying: queue.length ? s.isPlaying : false
		});
	},
	jumpTo: (i) => {
		const s = get();
		const track = s.queue[i];
		if (!track) return;
		set({
			index: i,
			currentTime: 0,
			isPlaying: true,
			recents: remember(s.recents, track)
		});
	},
	setExpanded: (v) => set({
		expanded: v,
		queueOpen: v ? get().queueOpen : false
	}),
	setQueueOpen: (v) => set({ queueOpen: v }),
	setLyricsOpen: (v) => set({ lyricsOpen: v }),
	createPlaylist: (name, tracks = []) => {
		const id = typeof crypto !== "undefined" && crypto.randomUUID ? crypto.randomUUID() : `pl_${Date.now()}`;
		set({ playlists: [{
			id,
			name: name.trim() || "New playlist",
			tracks,
			createdAt: Date.now()
		}, ...get().playlists] });
		return id;
	},
	addToPlaylist: (id, track) => {
		set({ playlists: get().playlists.map((p) => p.id === id && !p.tracks.some((t) => t.id === track.id) ? {
			...p,
			tracks: [track, ...p.tracks]
		} : p) });
	},
	removeFromPlaylist: (id, trackId) => {
		set({ playlists: get().playlists.map((p) => p.id === id ? {
			...p,
			tracks: p.tracks.filter((t) => t.id !== trackId)
		} : p) });
	},
	deletePlaylist: (id) => {
		if (id === "likes") return;
		set({ playlists: get().playlists.filter((p) => p.id !== id) });
	},
	rememberSearch: (q) => {
		const v = q.trim();
		if (!v) return;
		set({ recentSearches: [v, ...get().recentSearches.filter((s) => s !== v)].slice(0, 8) });
	},
	clearRecents: () => set({ recents: [] }),
	setHydrated: () => set({ hydrated: true })
}), {
	name: "sonara-v1",
	storage: createJSONStorage(() => typeof window === "undefined" ? {
		getItem: () => null,
		setItem: () => {},
		removeItem: () => {}
	} : localStorage),
	partialize: (s) => ({
		likedIds: s.likedIds,
		recents: s.recents,
		playlists: s.playlists,
		recentSearches: s.recentSearches,
		volume: s.volume
	}),
	onRehydrateStorage: () => (state) => {
		state?.setHydrated();
	}
}));
//#endregion
//#region src/components/cover.tsx
function Cover({ src, alt, title, className, rounded = "md" }) {
	const [failed, setFailed] = useState(false);
	useEffect(() => {
		setFailed(false);
	}, [src]);
	const radius = rounded === "full" ? "rounded-full" : rounded === "lg" ? "rounded-lg" : rounded === "sm" ? "rounded-sm" : "rounded-md";
	const showImg = src && !failed;
	return /* @__PURE__ */ jsx("div", {
		className: cn("relative overflow-hidden bg-chip", radius, className),
		style: showImg ? void 0 : { background: `linear-gradient(145deg, hsl(${hashHue(title || alt)} 28% 22%), hsl(${hashHue(title || alt)} 18% 10%))` },
		children: showImg ? /* @__PURE__ */ jsx("img", {
			src,
			alt,
			className: "h-full w-full object-cover",
			loading: "lazy",
			onError: () => setFailed(true)
		}) : /* @__PURE__ */ jsx("span", {
			className: "absolute inset-0 grid place-items-center text-lg font-semibold text-fg/80",
			children: (title || alt).slice(0, 1).toUpperCase()
		})
	});
}
//#endregion
export { usePlayer as n, Cover as t };
