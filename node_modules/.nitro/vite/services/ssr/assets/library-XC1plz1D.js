import { n as cn, t as Button } from "./button-D6cJemEt.js";
import { n as usePlayer, t as Cover } from "./cover-OSg2m9eM.js";
import { u as TrackRow } from "./router-B0lYCnsa.js";
import { t as AlbumCard } from "./cards-5QXYixfg.js";
import { t as Rail } from "./rail-D5jx3uJq.js";
import { useMemo, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/library.tsx?tsr-split=component
function LibraryPage() {
	const recents = usePlayer((s) => s.recents);
	const likedIds = usePlayer((s) => s.likedIds);
	const playlists = usePlayer((s) => s.playlists);
	const playTracks = usePlayer((s) => s.playTracks);
	const createPlaylist = usePlayer((s) => s.createPlaylist);
	const deletePlaylist = usePlayer((s) => s.deletePlaylist);
	const [tab, setTab] = useState("recents");
	const likedTracks = useMemo(() => {
		const fromPlaylists = playlists.find((p) => p.id === "likes")?.tracks ?? [];
		const fromRecents = recents.filter((t) => likedIds.includes(t.id));
		const map = new Map(fromPlaylists.map((t) => [t.id, t]));
		for (const t of fromRecents) map.set(t.id, t);
		return likedIds.map((id) => map.get(id)).filter((t) => Boolean(t));
	}, [
		likedIds,
		playlists,
		recents
	]);
	const userPlaylists = useMemo(() => playlists.filter((p) => p.id !== "likes").map((p) => ({
		id: p.id,
		name: p.name,
		artwork: p.tracks[0]?.artworkLg || p.tracks[0]?.artwork || null,
		artworkLg: p.tracks[0]?.artworkLg || null,
		trackCount: p.tracks.length,
		isAlbum: false,
		tracks: p.tracks
	})), [playlists]);
	return /* @__PURE__ */ jsxs("div", {
		className: "stagger-in space-y-8 px-4 py-6 md:px-8",
		children: [
			/* @__PURE__ */ jsxs("header", {
				className: "flex flex-wrap items-end justify-between gap-4",
				children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
					className: "text-xs font-medium tracking-[0.18em] text-subtle uppercase",
					children: "Yours"
				}), /* @__PURE__ */ jsx("h1", {
					className: "mt-1 text-3xl font-semibold tracking-tight",
					children: "Library"
				})] }), /* @__PURE__ */ jsx(Button, {
					variant: "chip",
					size: "sm",
					onClick: () => createPlaylist(`Playlist ${playlists.length + 1}`),
					children: "New playlist"
				})]
			}),
			/* @__PURE__ */ jsx("div", {
				className: "flex gap-2 border-b border-border/40 pb-3 overflow-x-auto [scrollbar-width:none] -mx-4 px-4 sm:mx-0 sm:px-0",
				children: [
					["recents", "Recents"],
					["likes", "Liked"],
					["playlists", "Playlists"]
				].map(([id, label]) => /* @__PURE__ */ jsx("button", {
					type: "button",
					className: cn("rounded-full px-4 py-2 text-xs md:text-sm font-semibold transition-all duration-150 active:scale-95 shrink-0", tab === id ? "bg-elevated/90 text-fg border border-border/60 shadow-sm" : "bg-chip/60 text-muted hover:bg-chip hover:text-fg"),
					onClick: () => setTab(id),
					children: label
				}, id))
			}),
			tab === "recents" ? recents.length ? /* @__PURE__ */ jsx(Rail, {
				title: "Recently played",
				children: recents.map((t) => /* @__PURE__ */ jsx(AlbumCard, {
					track: t,
					queue: recents
				}, t.id))
			}) : /* @__PURE__ */ jsx(Empty, { text: "Play something and it will land here." }) : null,
			tab === "likes" ? likedTracks.length ? /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs("div", {
				className: "mb-4 flex items-center justify-between",
				children: [/* @__PURE__ */ jsxs("p", {
					className: "text-sm text-muted",
					children: [likedTracks.length, " tracks"]
				}), /* @__PURE__ */ jsx(Button, {
					variant: "solid",
					size: "sm",
					onClick: () => playTracks(likedTracks, 0),
					children: "Play all"
				})]
			}), likedTracks.map((t, i) => /* @__PURE__ */ jsx(TrackRow, {
				track: t,
				index: i,
				queue: likedTracks
			}, t.id))] }) : /* @__PURE__ */ jsx(Empty, { text: "Tap the heart on a track to save it." }) : null,
			tab === "playlists" ? userPlaylists.length ? /* @__PURE__ */ jsx("div", {
				className: "grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5",
				children: playlists.filter((p) => p.id !== "likes").map((p) => /* @__PURE__ */ jsxs("div", {
					className: "group",
					children: [/* @__PURE__ */ jsxs("button", {
						type: "button",
						className: "w-full text-left",
						onClick: () => p.tracks.length && playTracks(p.tracks, 0),
						children: [
							/* @__PURE__ */ jsx(Cover, {
								src: p.tracks[0]?.artworkLg || p.tracks[0]?.artwork,
								alt: p.name,
								title: p.name,
								className: "aspect-square w-full"
							}),
							/* @__PURE__ */ jsx("p", {
								className: "mt-2 truncate text-sm font-medium",
								children: p.name
							}),
							/* @__PURE__ */ jsxs("p", {
								className: "text-xs text-muted",
								children: [p.tracks.length, " tracks"]
							})
						]
					}), /* @__PURE__ */ jsx("button", {
						type: "button",
						className: "mt-1 text-xs text-subtle hover:text-fg",
						onClick: () => deletePlaylist(p.id),
						children: "Remove"
					})]
				}, p.id))
			}) : /* @__PURE__ */ jsx(Empty, { text: "Create a playlist from any track menu." }) : null
		]
	});
}
function Empty({ text }) {
	return /* @__PURE__ */ jsx("div", {
		className: "rounded-xl bg-elevated px-6 py-16 text-center",
		children: /* @__PURE__ */ jsx("p", {
			className: "text-sm text-muted",
			children: text
		})
	});
}
//#endregion
export { LibraryPage as component };
